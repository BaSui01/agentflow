# Agent 模块架构重构计划（收口版）

> 文档类型：可执行架构重构收口计划
> 创建日期：2026-04-22
> 当前分支：`refactor/agent-architecture`
> 执行规则：单轨替换；不保留兼容分支；不保留双实现
> 测试规则：TDD（Red-Green-Refactor）
> 停止规则：存在 `- [ ]` 或验证未通过时不得宣布完成

> 当前状态（2026-04-24）：
> - `lint`：通过
> - `report`：`70 / 31 / 101`
> - `gate`：阻断（仍有 31 项 `[ ]`）
> - 结论：**本计划当前不可归档**；它现在主要承载 `agent/` root 瘦身与 Phase-6 收口任务，不负责“我的 Agent 框架”公开产品面切换。

---

## 0. 执行状态总览

- [x] `agent/` 的 8 个目标顶层目录已存在：`adapters/`、`capabilities/`、`collaboration/`、`core/`、`execution/`、`integration/`、`observability/`、`persistence/`
- [x] 计划涉及的旧顶层子目录集合已删除，当前目录口径已切到新分层
- [x] `agent` 子模块正式 runtime 入口已统一为 `agent/runtime.Builder`
- [x] 官方多 Agent facade 已统一为 `agent/team`，`teamadapter` 已统一到 `agent/adapters/teamadapter`
- [x] `agent/` 根目录非测试 Go 文件已进一步收口到 5 个：`base.go`、`builder.go`、`interfaces.go`、`registry.go`、`request.go`
- [x] `agent/...`、`go build ./...`、`go test ./...`、`scripts/arch_guard.ps1`、`agent/... -cover`、`agent/... -bench=. -benchmem` 已跑通
- [ ] `agent/` 根目录非测试 Go 行数 `< 5000`（当前实测 `12930`，Phase-6 将以更严 `< 2000` 目标取代本项）
- [ ] `golangci-lint run ./...` 未通过（已安装并执行，但仓库存在大量既存 lint 问题，超出本次收口范围）
- [x] `agent/` 顶层目录实际仅保留 `8 层 + 根文件`（当前实测仅保留 `adapters/capabilities/collaboration/core/execution/integration/observability/persistence` 8 个子目录；`TestAgentTopLevelDirectoryAllowlist` 与 `scripts/arch_guard.ps1` 已验证）
- [x] `agent/` 根目录非测试 Go 文件收口到 `5 个`（当前仅保留 `base.go / builder.go / interfaces.go / registry.go / request.go`）
- [ ] `agent/` 根目录非测试 Go 行数 `< 2000`（当前实测 `10968`；热点为 `request.go 2760`、`builder.go 2671`、`interfaces.go 1909`）

当前剩余顶层 `[ ]` 数量：**3**
全计划剩余 `[ ]` 数量：**31**

未完成项清单（按优先级归纳）：

- Phase-6 的 root LOC 收口 `< 2000`
- 将 `ChatCompletion/Execute/StreamCompletion` 等主执行逻辑继续下沉到 `agent/execution/*`
- 将 `LoopExecutor/ReasoningRuntime/LoopValidator/CompletionJudge` 等循环簇继续下沉
- 收紧 `TestAgentRootFilesBudget` / 新增 `TestAgentRootLinesOfCodeBudget`
- 同步 README / 迁移指南 / `scripts/arch_guard.ps1` 到最终 `8 层 + 5 根文件 + LOC < 2000` 口径

下一步执行项（仅来自现有未完成任务）：

1. 先补 `TestAgentRootLinesOfCodeBudget` 失败测试
2. 再优先下沉 `request.go` / `builder.go` 中最大职责簇
3. 最后收紧文档与守卫，过 `gate`

---

## 1. 当前收口事实

### 1.1 唯一正式入口

- 仓库级正式入口：`sdk.New(opts).Build(ctx)`
- `agent` 子模块正式 runtime 入口：`agent/runtime.Builder`
- `agent/builder.go` 仅保留高级扩展和包内构建核心语义，不再作为正式 runtime 入口宣传

### 1.2 目录与职责

- root public surface 只保留 9 个非测试文件，负责公开类型、公共运行时表面和向下分层的薄门面
- `agent/adapters/chat.go` 是唯一正式 ChatRequest 构造边界
- `agent/team/internal/engines/multiagent` 是多 Agent 协作正式实现归口
- `agent/team` 是官方多 Agent facade
- `agent/persistence/checkpoint` 是 checkpoint store 实现归口
- `agent/capabilities/tools` 是技能/工具发现与技能管理的正式归口

### 1.3 本轮额外收口

- `examples/08_low_priority_features/main.go` 已从旧 `collaboration.*` 命名统一到 `multiagent.*`
- `scripts/livecheck/advanced_features.go` 已从旧 `skills.*` / `collaboration.*` 命名统一到 `tools.*` / `multiagent.*`
- `api/handlers/agent_test.go`、`api/handlers/handlers_extra_test.go`、`benchmarks/agent_concurrency_bench_test.go`、`e2e/agent_execution_test.go`、`e2e/agent_tool_sandbox_test.go`、`workflow/dsl/orchestration_integration_test.go` 已清理旧 `discovery` / `collaboration` / `execution` 残留
- 已新增迁移指南：[docs/migration/agent-refactor-2026-04-22.md](/mnt/e/code/agentflow/docs/migration/agent-refactor-2026-04-22.md)

---

## 2. 测试策略（TDD）

- [x] 先写失败测试并确认红灯（验证命令：`rtk proxy "cmd.exe /c go test ./agent -run TestAgentRootFilesBudget -count=1"`；通过标准：收口前 root 文件预算失败，能够稳定暴露根包过厚问题）
- [x] 采用最小实现让测试转绿（验证命令：`rtk proxy "cmd.exe /c go test ./agent -run TestAgentRootFilesBudget -count=1"`；通过标准：root 非测试 Go 文件数收口到 9，预算守卫通过）
- [x] 完成重构并执行回归验证（验证命令：`rtk proxy "cmd.exe /c go test ./agent/... -count=1"`；通过标准：`agent/...` 全部通过，且 execution-options 守卫已切到当前 loop runtime 所在 root 文件）

---

## 3. 执行计划（Phase）

- [x] 当前收口阶段已按“结构与入口 -> root 薄面 -> 调用方与文档 -> 全量验收”推进（验证命令：`rtk python3 scripts/refactor_plan_guard.py report --target "agent模块架构重构-2026-04-22.md" --require-tdd --require-verifiable-completion`；通过标准：Phase 总览与文档内各阶段状态一致）

### Phase-1：结构与入口收口

- [x] 完成 8 层目录建立与旧目录删除（验证命令：`rtk proxy "cmd.exe /c go test ./agent -run TestAgentArchitectureLayering -count=1"`；通过标准：目录守卫通过，旧目录集合不再存在）
- [x] 完成正式 runtime 入口口径统一（验证命令：`rtk proxy "cmd.exe /c go test . -run TestAgentOfficialRuntimeEntrypointDocs -count=1"`；通过标准：文档守卫通过，正式 runtime 入口统一为 `agent/runtime.Builder`）
- [x] 完成多 Agent facade 口径统一（验证命令：`rtk proxy "cmd.exe /c go test . -run TestPublicProductSurfaceDocsExamplesConsistency -count=1"`；通过标准：`agent/team` 继续作为官方多 Agent facade）

### Phase-2：Root 薄面收口

- [x] 将 root 非测试 Go 文件压缩到 9 个（验证命令：`rtk proxy "cmd.exe /c go test ./agent -run TestAgentRootFilesBudget -count=1"`；通过标准：root 文件预算守卫通过，当前 root 文件集合与计划 allowlist 一致）
- [x] 将闭环执行守卫切到合并后的 root 文件（验证命令：`rtk proxy "cmd.exe /c go test . -run TestAgentExecutionOptionsArchitectureGuards -count=1"`；通过标准：当前 loop runtime 所在 root 文件通过 legacy control fallback 守卫，`agent/request.go` 继续保持 ChatRequest 不直接构造）
- [ ] 将 root 非测试 Go 行数压到 `< 5000`（验证命令：`rtk python3 - <<'PY'\nfrom pathlib import Path\nfiles=[p for p in Path('agent').glob('*.go') if not p.name.endswith('_test.go')]\nprint(sum(sum(1 for _ in p.open(encoding='utf-8')) for p in files))\nPY`；通过标准：统计值 `< 5000`，且不通过压缩可读性或引入双实现来“作弊”；当前状态：`EnhancedExecutionOptions` 与 feature/config 纯逻辑已下沉到 `agent/integration/features.go`，`RuntimeStreamEvent/Emitter` 事件模型已下沉到 `agent/observability/events/runtime_stream.go`，`Plan()` 的 numbered-plan tool schema/解析器已下沉到 `agent/capabilities/planning/numbered_plan_tool.go`，`applyInputContext` 已下沉到 `agent/execution/context/input_context.go`，`panicPayloadToError` 已下沉到 `agent/core/internal_helpers.go`，`completion/validation` root 适配层已迁到 `agent/interfaces.go`，`Observe` / `collectContextMemory` / `buildValidationFeedbackMessage` 已迁到 `agent/base.go`，实测 `12930`）

### Phase-3：调用方与文档同步

- [x] 完成 examples / livecheck / 测试中的旧命名残留清理（验证命令：`rtk proxy "cmd.exe /c go build ./..."`；通过标准：全仓构建通过，不再出现旧 `collaboration` / `discovery` / `execution` 命名缺口）
- [x] 完成 README / README_EN 目录树同步（验证命令：`rtk proxy "cmd.exe /c go test . -run TestAgentOfficialRuntimeEntrypointDocs -count=1" && rtk proxy "cmd.exe /c go test . -run TestPublicProductSurfaceDocsExamplesConsistency -count=1"`；通过标准：入口与产品表面文档守卫全部通过）
- [x] 完成迁移指南落档（验证命令：`rtk proxy "cmd.exe /c if exist docs\\migration\\agent-refactor-2026-04-22.md (exit 0) else (exit 1)"`；通过标准：迁移指南文件存在并描述正式入口、目录口径和旧路径替换）
- [x] 完成计划文档改写为收口版（验证命令：`rtk python3 scripts/refactor_plan_guard.py lint --target "agent模块架构重构-2026-04-22.md" --require-tdd --require-verifiable-completion`；通过标准：计划文档结构合法，章节与验证命令完整）

### Phase-5：顶层目录清零（删除空残留目录，`agent/` 仅保留 8 层）

#### 5.1 职责矩阵（空目录处置）

| 目录 | 当前状态 | 处置 |
|---|---|---|
| `agent/artifacts/` `agent/context/` `agent/conversation/` `agent/declarative/` `agent/deployment/` `agent/evaluation/` `agent/handoff/` `agent/hitl/` `agent/hosted/` `agent/k8s/` `agent/lsp/` `agent/multiagent/` `agent/orchestration/` `agent/planner/` `agent/reasoning/` `agent/runtime/` `agent/skills/` `agent/streaming/` `agent/structured/` `agent/voice/` | 0 个 `.go` 文件 | **删除** |
| `agent/internalcore/` | 3 个 `.go` 文件，共 470 行 | **按职责下沉到 `agent/core/` / `agent/execution/loop/`，随后删除目录** |
| `agent/adapters/` `agent/capabilities/` `agent/collaboration/` `agent/core/` `agent/execution/` `agent/integration/` `agent/observability/` `agent/persistence/` | 目标 8 层 | **保留** |

#### 5.2 TDD 路径（Red-Green-Refactor）

- [x] 先写失败测试：扩展 `agent/architecture_refactor_test.go` 新增 `TestAgentTopLevelDirectoryAllowlist`，只允许 `adapters/capabilities/collaboration/core/execution/integration/observability/persistence` 8 个顶层目录存在（验证命令：`rtk proxy "cmd.exe /c go test ./agent -run TestAgentTopLevelDirectoryAllowlist -count=1"`；通过标准：测试红灯，报告 21 个残留空目录 + `internalcore`）
- [x] 最小实现让测试转绿：按下方 5.3 清单删除 21 个空目录、处置 `internalcore`，使 `TestAgentTopLevelDirectoryAllowlist` 转绿（验证命令：`rtk proxy "cmd.exe /c go test ./agent -run TestAgentTopLevelDirectoryAllowlist -count=1"`；通过标准：测试绿灯，`agent/` 顶层子目录集合 `== {adapters,capabilities,collaboration,core,execution,integration,observability,persistence}`）
- [x] 重构并回归验证：完成 Phase-5 相关守卫、构建与文档回归（验证命令：`rtk proxy "cmd.exe /c go build ./..." && rtk proxy "cmd.exe /c go test ./agent -run \"TestAgentArchitectureLayering|TestAgentTopLevelDirectoryAllowlist|TestAgentDependencyDirection\" -count=1" && rtk proxy "cmd.exe /c go test . -run \"TestPublicProductSurfaceDocsExamplesConsistency|TestAgentOfficialRuntimeEntrypointDocs\" -count=1" && rtk proxy "cmd.exe /c powershell.exe -ExecutionPolicy Bypass -File scripts/arch_guard.ps1"`；通过标准：Phase-5 相关守卫全部通过，且 `TestAgentDependencyDirection` 仅剩既存 `core -> execution/persistence` 两条遗留违规，未新增新违规）

#### 5.3 删除/合并清单（具体路径）

- [x] 删除 `agent/artifacts/`（验证命令：`rtk proxy "cmd.exe /c if exist agent\\artifacts (exit 1) else (exit 0)"`；通过标准：目录不存在）
- [x] 删除 `agent/context/`（验证命令同上，替换路径；通过标准：目录不存在）
- [x] 删除 `agent/conversation/`（验证命令同上；通过标准：目录不存在）
- [x] 删除 `agent/declarative/`（验证命令同上；通过标准：目录不存在）
- [x] 删除 `agent/deployment/`（验证命令同上；通过标准：目录不存在）
- [x] 删除 `agent/evaluation/`（验证命令同上；通过标准：目录不存在）
- [x] 删除 `agent/handoff/`（验证命令同上；通过标准：目录不存在）
- [x] 删除 `agent/hitl/`（验证命令同上；通过标准：目录不存在）
- [x] 删除 `agent/hosted/`（验证命令同上；通过标准：目录不存在）
- [x] 删除 `agent/k8s/`（验证命令同上；通过标准：目录不存在）
- [x] 删除 `agent/lsp/`（验证命令同上；通过标准：目录不存在）
- [x] 删除 `agent/multiagent/`（验证命令同上；通过标准：目录不存在。注意：`agent/team/internal/engines/multiagent/` 是归口实现，不要误删）
- [x] 删除 `agent/orchestration/`（验证命令同上；通过标准：目录不存在）
- [x] 删除 `agent/planner/`（验证命令同上；通过标准：目录不存在）
- [x] 删除 `agent/reasoning/`（验证命令同上；通过标准：目录不存在。注意：`reasoning` 顶级包归属于仓库根 `reasoning/`，不是 `agent/reasoning/`，确认无 import 引用后删除）
- [x] 删除 `agent/runtime/`（验证命令同上；通过标准：目录不存在。注意：`agent/runtime/` 才是正式 runtime 归口）
- [x] 删除 `agent/skills/`（验证命令同上；通过标准：目录不存在。注意：`agent/capabilities/tools/` 是技能归口）
- [x] 删除 `agent/streaming/`（验证命令同上；通过标准：目录不存在）
- [x] 删除 `agent/structured/`（验证命令同上；通过标准：目录不存在）
- [x] 删除 `agent/voice/`（验证命令同上；通过标准：目录不存在）
- [x] 处置 `agent/internalcore/`：将 3 文件内容按职责合并到 `agent/core/` / `agent/execution/loop/`，随后删除目录（验证命令：`rtk proxy "cmd.exe /c if exist agent\\internalcore (exit 1) else (exit 0)" && rtk proxy "cmd.exe /c go build ./..."`；通过标准：目录不存在且全仓构建通过）

#### 5.4 守卫同步

- [x] 更新 `agent/architecture_refactor_test.go` 中 `TestAgentArchitectureLayering` 的 `oldDirs` 集合，加入本轮删除的 21 个目录做防回潮（验证命令：`rtk proxy "cmd.exe /c go test ./agent -run TestAgentArchitectureLayering -count=1"`；通过标准：测试绿灯，oldDirs 集合已扩展）
- [x] `scripts/arch_guard.ps1` 增加空目录回潮检测（验证命令：`rtk proxy "cmd.exe /c powershell.exe -ExecutionPolicy Bypass -File scripts/arch_guard.ps1"`；通过标准：脚本返回 0 且覆盖空目录检测断言）
- [x] 同步 `README.md` / `README_EN.md` 中 `agent/` 目录树示意（验证命令：`rtk proxy "cmd.exe /c go test . -run TestPublicProductSurfaceDocsExamplesConsistency -count=1"`；通过标准：产品表面一致性守卫通过）

---

### Phase-6：Root 大文件按职责簇下沉（9 → 5 文件，LOC → < 2000）

#### 6.1 唯一入口与保留职责声明

- 仓库级正式入口不变：`sdk.New(opts).Build(ctx)`
- `agent` 子模块正式 runtime 入口不变：`agent/runtime.Builder`
- `agent/builder.go` 只保留 `AgentBuilder` 本体 + `WithXxx` 配置表面，不再包含 `ExecutionPipeline/Middleware/reasoning registry`
- root 最终保留 5 个文件及其唯一归口职责：
  - `base.go`：`Agent` 接口、核心 DTO（`Input/Output/Feedback/LoopState/PlanResult`）、状态机常量
  - `builder.go`：`AgentBuilder` 本体及 `WithXxx` 入口
  - `interfaces.go`：对外的 `*Runner` 能力接口（ReflectionRunner / DynamicToolSelectorRunner / PromptEnhancerRunner / SkillDiscoverer / MCPServerRunner / LSPClientRunner / EnhancedMemoryRunner / ObservabilityRunner）
  - `registry.go`：`AgentRegistry` 本体（工厂注册/查询/内置类型初始化）
  - `request.go`：`RunConfig` + `ResolveRunConfig/MergeRunConfig` 公共入口

#### 6.2 重复语义与下沉归口（删除 4 个文件、迁移 N 个职责簇）

| 下沉源 | 簇 | 目标归口 | 备注 |
|---|---|---|---|
| `request.go` | `BaseAgent.ChatCompletion/Plan/Execute/StreamCompletion` | `agent/execution/* helpers + root 薄门面` | 执行流程层 |
| `builder.go` | `LoopExecutor/ReasoningRuntime/LoopValidator*/CompletionJudge/*LoopValidator` | `agent/execution/loop/` | 循环与校验 |
| `request.go` / `interfaces.go` | `Runtime*StreamEvent / SDKStreamEventType / SDKHandoffOccured / RuntimeStreamEmitter` | `agent/observability/events/` | 流式事件 |
| `builder.go` / `agent/integration/features.go` | `BaseAgent.EnableXxx / ExecuteEnhanced / EnhancedExecutionOptions / GetFeatureStatus / ExportConfiguration` | `agent/integration/features.go` | 能力集成开关 |
| `builder.go` | `ExecutionPipeline / ExecutionMiddleware / enhancedCtxKey / withSkillInstructions` | `agent/execution/pipeline.go` | 执行管道 |
| `builder.go` | `NewDefaultReasoningRegistry / NewReasoningRegistryForExposure / registerDefaultReasoningPattern / reasoningToolSchemas / registerReasoningPatternsForExposure` | `agent/core/reasoning_registry.go` | 推理注册 |
| `interfaces.go` | `PersistenceStores / ScopedPersistenceStores / PromptDocument / ConversationDoc / RunDoc` | `agent/persistence/stores.go` | 持久化实现 |
| `interfaces.go` | `AgentTool / AgentToolConfig / agentToolArgs` | `agent/capabilities/tools/agent_as_tool.go` | agent 作为工具 |
| `interfaces.go` | `TeamMember / TeamResult / TeamOption / TeamOptions` | `agent/team/types.go` | 多 Agent facade 类型 |
| `interfaces.go` | `ProviderAdapter / ToolExecutorAdapter / filterToolSchemasByWhitelist` | `agent/adapters/llm_tool.go` | 适配层 |
| `request.go` | `runtimeHandoffExecutor / runtimeHandoffAgentAdapter / RuntimeHandoffTarget / WithRuntimeHandoffTargets / parseRuntimeHandoffCall / emitRuntimeHandoffAgentUpdated` | `agent/collaboration/handoff/runtime.go` | 运行期 handoff |
| `request.go` | `PreparedToolProtocol / ToolProtocolRuntime / DefaultToolProtocolRuntime / preparedRequest / BaseAgent.prepareChatRequest` | `agent/execution/tool_protocol.go` | 工具协议运行期 |
| `request.go` | `EphemeralPromptLayerBuilder / EphemeralPromptLayerInput` | `agent/core/prompt_layer.go` | prompt 层构建 |
| `registry.go` | `CachingResolver` + `WithXxx` | `agent/core/resolver.go` | 解析与缓存 |
| `registry.go` | `LifecycleManager / HealthStatus` | `agent/execution/lifecycle.go` | 生命周期 |
| `checkpoint_binding.go` | `CheckpointManager` + autosave/rollback/diff/版本列表 | `agent/persistence/checkpoint/manager.go` | checkpoint 管理 |
| `checkpoint_binding.go` | `LoopState.CheckpointVariables/PopulateCheckpoint/restoreFromContext/ApplyValidationResult` + `summarize*` 辅助 | `agent/persistence/checkpoint/loop_state_binding.go` | 循环状态绑定 |
| `prompt_bundle.go` | `PromptEnhancer / PromptEnhancerConfig / PromptOptimizer` | `agent/capabilities/prompt/enhancer.go` | prompt 增强器 |
| `prompt_bundle.go` | `PromptBundle / SystemPrompt / Example / MemoryConfig / PlanConfig / ReflectionConfig` + 渲染 | `agent/capabilities/prompt/bundle.go` | prompt bundle 本体 |
| `defensive_prompt.go` | 全部（`DefensivePromptEnhancer` 等） | `agent/capabilities/prompt/defensive.go` | 安全 prompt |
| `base.go` | `CodeValidator / CodeValidationLanguage` | `agent/execution/code_validator.go` | 代码校验 |

**已从 root 删除的旧文件**：`integration.go`

#### 6.3 TDD 路径（Red-Green-Refactor）

- [x] 先写失败测试：收紧 `TestAgentRootFilesBudget` 预算从 9 收紧到 5，并新增 `TestAgentRootLinesOfCodeBudget`，目标 `< 2000`（验证命令：`rtk proxy "cmd.exe /c go test ./agent -run TestAgentRootFilesBudget -count=1" && rtk proxy "cmd.exe /c go test ./agent -run TestAgentRootLinesOfCodeBudget -count=1"`；通过标准：两个测试均红灯，分别报告 `9 > 5` 和 `14046 > 2000`）
- [ ] 最小实现让测试转绿：按 6.2 清单迁移簇，让 root 仅剩 `base.go / builder.go / interfaces.go / registry.go / request.go` 且 LOC `< 2000`（验证命令：同上两条；通过标准：两个测试均绿灯，root 仅剩 5 文件，LOC 统计值 `< 2000`）
- [ ] 重构并回归验证：`agent/...` 全绿 + 全仓构建 + 架构守卫通过（验证命令：`rtk proxy "cmd.exe /c go test ./agent/... -count=1" && rtk proxy "cmd.exe /c go build ./..." && rtk proxy "cmd.exe /c powershell.exe -ExecutionPolicy Bypass -File scripts/arch_guard.ps1"`；通过标准：全部返回 0，无回潮）

#### 6.4 逐簇迁移任务（单轨替换，不保留双实现）

- [ ] 下沉 `BaseAgent.ChatCompletion/Plan/Execute/Observe/StreamCompletion` → `agent/execution/* helpers + root 薄门面`（验证命令：`rtk proxy "cmd.exe /c go test ./agent/execution/... ./agent -count=1"`；通过标准：测试通过，root 仅保留必要 public 方法；当前状态：`Observe` 本体及其 `collectContextMemory` / `buildValidationFeedbackMessage` 辅助已迁到 `agent/base.go`，`Plan` 的 numbered-plan tool schema/解析器已迁到 `agent/capabilities/planning/numbered_plan_tool.go`，`ChatCompletion/Execute/StreamCompletion` 主体当前在 `agent/request.go`）
- [ ] 下沉 `LoopExecutor/ReasoningRuntime/LoopValidator*/CompletionJudge` → `agent/execution/loop/`（验证命令：`rtk proxy "cmd.exe /c go test ./agent/execution/loop/... -count=1"`；通过标准：测试通过，root 不再定义这些类型；当前状态：`LoopValidator*/CompletionJudge` 的 root 适配与 `root↔execution/loop` 转换层已迁到 `agent/interfaces.go`，`LoopExecutor/ReasoningRuntime` 执行编排簇当前在 `agent/builder.go`，真实判停/校验核心仍以 `agent/execution/loop/completion.go` 为真源）
- [ ] 下沉 `Runtime*StreamEvent/SDKStreamEventType` → `agent/observability/events/`（验证命令：`rtk proxy "cmd.exe /c go test ./agent/observability/... -count=1"`；通过标准：测试通过，root 仅保留对这些类型的再导出别名或完全移除；当前状态：事件模型、SDK 事件常量和 emitter context 存取已迁入 `agent/observability/events/runtime_stream.go`，root 当前仅保留别名与薄 helper）
- [ ] 下沉 `EnableXxx / ExecuteEnhanced / EnhancedExecutionOptions` → `agent/integration/features.go`（验证命令：`rtk proxy "cmd.exe /c go test . -run TestAgentExecutionOptionsArchitectureGuards -count=1"`；通过标准：execution-options 守卫已切到新路径且通过；当前状态：`EnhancedExecutionOptions`、默认值以及 `GetFeatureStatus/GetFeatureMetrics/ExportConfiguration` 的纯逻辑已迁入 `agent/integration/features.go`，`EnableXxx / ExecuteEnhanced` 与 `configuredExecutionOptions/coreExecutor` 当前在 `agent/builder.go`，后续仍需继续下沉到 8 层目录）
- [ ] 下沉 `ExecutionPipeline/ExecutionMiddleware` → `agent/execution/pipeline.go`（验证命令：`rtk proxy "cmd.exe /c go test ./agent/execution/... -count=1"`；通过标准：测试通过）
- [ ] 下沉 `NewDefaultReasoningRegistry` 等 → `agent/core/reasoning_registry.go`（验证命令：`rtk proxy "cmd.exe /c go test ./agent/core/... -count=1"`；通过标准：测试通过）
- [ ] 下沉 `PersistenceStores / ScopedPersistenceStores / PromptDocument / ConversationDoc / RunDoc` → `agent/persistence/stores.go`（验证命令：`rtk proxy "cmd.exe /c go test ./agent/persistence/... -count=1"`；通过标准：测试通过）
- [ ] 下沉 `AgentTool` → `agent/capabilities/tools/agent_as_tool.go`（验证命令：`rtk proxy "cmd.exe /c go test ./agent/capabilities/tools/... -count=1"`；通过标准：测试通过）
- [ ] 下沉 `TeamMember/TeamResult/TeamOption` → `agent/team/types.go`（验证命令：`rtk proxy "cmd.exe /c go test ./agent/team/... -count=1"`；通过标准：测试通过）
- [ ] 下沉 `ProviderAdapter/ToolExecutorAdapter` → `agent/adapters/llm_tool.go`（验证命令：`rtk proxy "cmd.exe /c go test ./agent/adapters/... -count=1"`；通过标准：测试通过）
- [ ] 下沉 `runtimeHandoffExecutor` 全家族 → `agent/collaboration/handoff/runtime.go`（验证命令：`rtk proxy "cmd.exe /c go test ./agent/collaboration/... -count=1"`；通过标准：测试通过，root 不再定义 handoff runtime 结构）
- [ ] 下沉 `ToolProtocolRuntime/preparedRequest/prepareChatRequest` → `agent/execution/tool_protocol.go`（验证命令：`rtk proxy "cmd.exe /c go test ./agent/execution/... ./agent -run TestAgentChatRequestConstructionBoundary -count=1"`；通过标准：测试通过，`agent/adapters/chat.go` 仍是唯一正式 ChatRequest 构造边界）
- [ ] 下沉 `EphemeralPromptLayerBuilder` → `agent/core/prompt_layer.go`（验证命令：`rtk proxy "cmd.exe /c go test ./agent/core/... -count=1"`；通过标准：测试通过）
- [ ] 下沉 `CachingResolver` → `agent/core/resolver.go`（验证命令：`rtk proxy "cmd.exe /c go test ./agent/core/... -count=1"`；通过标准：测试通过）
- [ ] 下沉 `LifecycleManager/HealthStatus` → `agent/execution/lifecycle.go`（验证命令：`rtk proxy "cmd.exe /c go test ./agent/execution/... -count=1"`；通过标准：测试通过）
- [x] 下沉 `CheckpointManager` + loop state binding → `agent/persistence/checkpoint/manager.go` + root 薄包装/绑定方法（验证命令：`rtk proxy "cmd.exe /c go test ./agent/... -run \"TestCheckpoint|TestFileCheckpointStore|TestLoopState\" -count=1"`；通过标准：checkpoint 定向测试通过，`agent/checkpoint_binding.go` 已删除，store/manager 真源迁入 `agent/persistence/checkpoint/`）
- [x] 下沉 `PromptEnhancer/PromptOptimizer` + `PromptBundle/SystemPrompt` + `DefensivePromptEnhancer` → `agent/capabilities/prompt/{bundle,enhancer,defensive}.go`（验证命令：`rtk proxy "cmd.exe /c go test ./agent -run \"TestPrompt|TestDefensivePrompt\" -count=1" && rtk proxy "cmd.exe /c go build ./agent/..."`；通过标准：prompt 定向测试通过，`agent/capabilities/prompt/` 已成为真实实现归口，root 仅保留别名/薄门面）
- [ ] 下沉 `CodeValidator/CodeValidationLanguage` → `agent/execution/code_validator.go`（验证命令：`rtk proxy "cmd.exe /c go test ./agent/execution/... -count=1"`；通过标准：测试通过）

#### 6.5 旧文件删除（单轨替换）

- [x] 删除 `agent/integration.go`（验证命令：`rtk proxy "cmd.exe /c if exist agent\\integration.go (exit 1) else (exit 0)" && rtk proxy "cmd.exe /c go build ./..."`；通过标准：文件不存在且全仓构建通过）
- [x] 删除 `agent/checkpoint_binding.go`（验证命令同上，替换文件名；通过标准：文件不存在且全仓构建通过）
- [x] 删除 `agent/prompt_bundle.go`（验证命令同上；通过标准：文件不存在且全仓构建通过）
- [x] 删除 `agent/defensive_prompt.go`（验证命令同上；通过标准：文件不存在且全仓构建通过）

#### 6.6 守卫与文档同步

- [ ] 收紧 `TestAgentRootFilesBudget` 预算 `9 → 5`，allowlist 固定为 `{base.go, builder.go, interfaces.go, registry.go, request.go}`（验证命令：`rtk proxy "cmd.exe /c go test ./agent -run TestAgentRootFilesBudget -count=1"`；通过标准：测试通过且 allowlist 与实测一致）
- [ ] 新增 `TestAgentRootLinesOfCodeBudget`：LOC 守卫 `< 2000`（验证命令：`rtk proxy "cmd.exe /c go test ./agent -run TestAgentRootLinesOfCodeBudget -count=1"`；通过标准：测试通过且统计值 `< 2000`）
- [ ] 更新 `scripts/arch_guard.ps1` 调用新 root 预算守卫与 LOC 守卫（验证命令：`rtk proxy "cmd.exe /c powershell.exe -ExecutionPolicy Bypass -File scripts/arch_guard.ps1"`；通过标准：脚本返回 0 且覆盖两个新守卫）
- [ ] 同步 `docs/migration/agent-refactor-2026-04-22.md` 追加 Phase-5/Phase-6 迁移指引（验证命令：`rtk python3 scripts/refactor_plan_guard.py lint --target "agent模块架构重构-2026-04-22.md" --require-tdd --require-verifiable-completion`；通过标准：计划 lint 通过）
- [ ] 同步 `README.md` / `README_EN.md` 的 `agent/` 目录树到 `8 层 + 5 根文件` 口径（验证命令：`rtk proxy "cmd.exe /c go test . -run TestPublicProductSurfaceDocsExamplesConsistency -count=1"`；通过标准：产品表面一致性守卫通过）

---

### Phase-4：全量验收

- [x] 完成 `agent/...` 全量测试（验证命令：`rtk proxy "cmd.exe /c go test ./agent/... -count=1"`；通过标准：`agent/...` 全绿）
- [x] 完成 `agent/...` 覆盖率口径（验证命令：`rtk proxy "cmd.exe /c go test ./agent/... -cover"`；通过标准：命令通过，`agent` 根包覆盖率实测 `83.5%`，未低于当前分支基线）
- [x] 完成 `agent/...` benchmark 口径（验证命令：`rtk proxy "cmd.exe /c go test ./agent/... -run=^$ -bench=. -benchmem"`；通过标准：benchmark 命令返回 0，关键包 benchmark 能正常输出）
- [x] 完成全仓构建（验证命令：`rtk proxy "cmd.exe /c go build ./..."`；通过标准：`go build ./...` 返回 0）
- [x] 完成全仓测试（验证命令：`rtk proxy "cmd.exe /c go test ./... -count=1"`；通过标准：`go test ./...` 返回 0）
- [x] 完成 PowerShell 架构守卫（验证命令：`rtk proxy "cmd.exe /c powershell.exe -ExecutionPolicy Bypass -File scripts/arch_guard.ps1"`；通过标准：脚本返回 0 且 focused architecture guard tests 通过）
- [ ] 完成 lint 口径（验证命令：`cmd.exe /c D:/go/bin/golangci-lint.exe run ./...`；通过标准：lint 返回 0；当前状态：命令已执行，但仓库存在大量既存 gofmt/gocognit/gocritic/gosec 等问题，尚未清零）

---

## 4. 完成定义（DoD）

- [x] 目录结构已完成（验证命令：`rtk proxy "cmd.exe /c go test ./agent -run TestAgentArchitectureLayering -count=1"`；通过标准：8 层目录存在，旧目录集合已删除）
- [x] 正式入口已完成（验证命令：`rtk proxy "cmd.exe /c go test . -run TestAgentOfficialRuntimeEntrypointDocs -count=1"`；通过标准：正式 runtime 入口统一为 `agent/runtime.Builder`）
- [x] 文档与产品表面已完成（验证命令：`rtk proxy "cmd.exe /c go test . -run TestPublicProductSurfaceDocsExamplesConsistency -count=1"`；通过标准：官方多 Agent facade、README 与教程口径一致）
- [x] Root 文件预算已完成（验证命令：`rtk proxy "cmd.exe /c go test ./agent -run TestAgentRootFilesBudget -count=1"`；通过标准：root 非测试 Go 文件数为 9）
- [x] 代码回归已完成（验证命令：`rtk proxy "cmd.exe /c go test ./agent/... -count=1" && rtk proxy "cmd.exe /c go test ./... -count=1"`；通过标准：`agent/...` 与全仓测试全部通过）
- [x] 构建与守卫已完成（验证命令：`rtk proxy "cmd.exe /c go build ./..." && rtk proxy "cmd.exe /c powershell.exe -ExecutionPolicy Bypass -File scripts/arch_guard.ps1"`；通过标准：全仓构建成功，PowerShell 架构守卫通过）
- [x] 覆盖率与 benchmark 已完成（验证命令：`rtk proxy "cmd.exe /c go test ./agent/... -cover" && rtk proxy "cmd.exe /c go test ./agent/... -run=^$ -bench=. -benchmem"`；通过标准：覆盖率命令与 benchmark 命令均返回 0）
- [ ] Root LOC 目标已完成（验证命令：`rtk python3 - <<'PY'\nfrom pathlib import Path\nfiles=[p for p in Path('agent').glob('*.go') if not p.name.endswith('_test.go')]\nprint(sum(sum(1 for _ in p.open(encoding='utf-8')) for p in files))\nPY`；通过标准：root 非测试 Go 行数 `< 5000`；Phase-6 完成后应以更严目标 `< 2000` 取代）
- [ ] Lint 已完成（验证命令：`cmd.exe /c D:/go/bin/golangci-lint.exe run ./...`；通过标准：lint 返回 0）
- [x] 顶层目录清零已完成（验证命令：`rtk proxy "cmd.exe /c go test ./agent -run TestAgentTopLevelDirectoryAllowlist -count=1"`；通过标准：`agent/` 顶层子目录集合 `== {adapters,capabilities,collaboration,core,execution,integration,observability,persistence}`，无残留空目录）
- [x] Root 文件数收紧到 5 已完成（验证命令：`rtk proxy "cmd.exe /c go test ./agent -run TestAgentRootFilesBudget -count=1"`；通过标准：root 非测试 Go 文件数 `== 5`，allowlist 固定为 `{base.go, builder.go, interfaces.go, registry.go, request.go}`）
- [ ] Root LOC `< 2000` 已完成（验证命令：`rtk proxy "cmd.exe /c go test ./agent -run TestAgentRootLinesOfCodeBudget -count=1"`；通过标准：root 非测试 Go 行数 `< 2000`，且不通过压缩可读性或引入双实现来“作弊”）

---

## 5. 变更日志

- 2026-04-22：将 `agent/base_agent.go`、`agent/completion.go`、`agent/react.go`、`agent/loop_executor.go`、`agent/loop_executor_runtime.go`、`agent/ephemeral_prompt.go`、`agent/trace_feedback.go`、`agent/reflection.go`、`agent/tool_selector.go`、`agent/remote_tool_transport.go`、`agent/extension_registry.go`、`agent/loop_control_policy.go` 收口并回 9 个 root 保留文件
- 2026-04-22：同步 execution-options 守卫到当时合并后的 root loop runtime 片段
- 2026-04-22：同步 README / README_EN 的 Agent 目录树到当前 9 文件 + 8 层目录口径
- 2026-04-22：补齐 [agent-refactor-2026-04-22.md](/mnt/e/code/agentflow/docs/migration/agent-refactor-2026-04-22.md) 迁移指南
- 2026-04-22：修复 examples / livecheck / api-handlers / benchmarks / e2e / workflow-dsl 中的旧 `collaboration` / `discovery` / `execution` 命名残留，恢复全仓 build/test
- 2026-04-22：追加 Phase-5（顶层目录清零：删除 21 个空目录 + `internalcore` 合并）与 Phase-6（root 9 → 5 文件、LOC → `< 2000`、按职责簇下沉至 8 层），更新 Section 0 未完成项与 Section 4 DoD，新增守卫 `TestAgentTopLevelDirectoryAllowlist` 与 `TestAgentRootLinesOfCodeBudget` 目标
- 2026-04-22：完成 Phase-5 收尾：删除 20 个空目录、移除 `agent/internalcore/`、将 loop policy 下沉到 `agent/execution/loop/`、同步 `scripts/arch_guard.ps1` 与 README 目录树口径
- 2026-04-22：推进 Phase-6 第一个职责簇：删除 `agent/prompt_bundle.go` / `agent/defensive_prompt.go`，真实实现下沉到 `agent/capabilities/prompt/`，root 文件数从 `9 -> 7`，LOC 从 `14047 -> 13158`
- 2026-04-22：推进 Phase-6 第二个职责簇：删除 `agent/checkpoint_binding.go`，将 checkpoint store/manager 真源迁入 `agent/persistence/checkpoint/`，root 文件数从 `7 -> 6`
- 2026-04-23：推进 Phase-6 第三个职责簇：将 `EnableXxx / ExecuteEnhanced / configuredExecutionOptions / coreExecutor / LoopExecutor / ReasoningRuntime` 从 `agent/integration.go` 并回 `agent/builder.go`，并同步 `agent/execution_options_guard_test.go` 与 `architecture_guard_test.go` 到新的 loop runtime 检查位置
- 2026-04-23：推进 Phase-6 第四个职责簇：将 `ChatCompletion / StreamCompletion / Plan / Execute / executeCore / prompt-context / resume helper` 从 `agent/integration.go` 并回 `agent/request.go`，删除 `agent/integration.go`，使 root 文件预算 `6 -> 5` 转绿
- 2026-04-23：继续推进 Phase-6：统一 `agent/core` 与 `agent/persistence/checkpoint` 的共享状态类型，删除 root checkpoint 转换胶水；将 reasoning selector 真源下沉到 `agent/execution/loop/reasoning_selector.go`，将 dynamic tool selector 纯算法下沉到 `agent/capabilities/tools/dynamic_selector_helpers.go`
- 2026-04-23：继续推进 Phase-6：将 `TraceFeedback*` 规划器与 `EphemeralPromptLayerBuilder` 真源下沉到 `agent/execution/context/{trace_feedback,ephemeral_prompt}.go`，`agent/request.go` 仅保留 BaseAgent 装配与 signal 采集包装
- 2026-04-23：推进 Phase-6 第五个职责簇：将 `ExecutionPipeline` 泛型与 runtime context key 下沉到 `agent/execution/pipeline.go` / `agent/execution/context/runtime_ctx.go`，并将 `PersistenceStores / ScopedPersistenceStores` 真源下沉到 `agent/persistence/stores.go`；root LOC 进一步压到 `11776`
- 2026-04-23：推进 Phase-6 第六个职责簇：将 `TraceFeedback*` 类型/规划器真源下沉到 `agent/execution/context/trace_feedback.go`，并让 `request.go` 改为 root alias + 薄门面；root LOC 进一步压到 `10968`

