# 仓库级统一复用与减冗余总计划-2026-04-26

> 文档类型：可执行仓库级重构计划
> 执行方式：单轨替换；不保留兼容双实现；不新增并行入口
> 当前状态：**已完成，可作为本轮实施记录归档/追踪**
> 替代关系：本计划已替代 `bootstrap-handler-usecase-重构计划-2026-04-26.md` 作为本轮统一复用治理的活跃执行源

## 执行状态总览

- [x] 已完成 Phase-0：新建仓库级总计划并冻结旧计划入口（验证命令：`python3 scripts/refactor_plan_guard.py lint --target '仓库级统一复用与减冗余总计划-2026-04-26.md' --require-tdd --require-verifiable-completion`；通过标准：计划文档结构合法，且旧计划已被标注为替代）
- [x] 已完成 Phase-1：execution mode 与 orchestration 输入适配收口（验证命令：`go test ./workflow/steps ./internal/usecase/... ./agent/team/... ./agent/runtime/orchestration/... -count=1`；通过标准：mode catalog 仍以 `agent/team` 为唯一真相来源，orchestration 输入适配测试转绿）
- [x] 已完成 Phase-2：service-backed handler 统一到 `BaseHandler[T]` 并抽离 request/error helper（验证命令：`go test ./api/handlers/... -count=1`；通过标准：除 `health.go` / `protocol.go` 外的 service-backed handler 全部编译通过且行为回归通过）
- [x] 已完成 Phase-3：`BuildServeHandlerSet(...)` 与 `buildStepDependencies(...)` 内部 builder 拆分（验证命令：`go test ./internal/app/bootstrap/... -count=1`；通过标准：唯一入口保留，bootstrap 包测试转绿）
- [x] 已完成 Phase-4：文档、守卫与治理规则同步（验证命令：`go test . -run 'TestWorkflowTeamBoundary|TestServiceBackedHandlersUseBaseHandler|TestExecutionModeCatalogSingleSource' -count=1`；通过标准：新增守卫通过，文档已写明新的唯一归口）

## 执行计划

- [x] Phase-0 计划治理：新增仓库级计划文档，并将旧 `bootstrap-handler-usecase` 计划标记为“被替代，不再作为活跃执行源”（验证命令：`grep -n '替代' docs/重构计划/bootstrap-handler-usecase-重构计划-2026-04-26.md docs/重构计划/仓库级统一复用与减冗余总计划-2026-04-26.md`；通过标准：两个文档都能说明替代关系）
- [x] Phase-1 执行模式收口：将 `normalizedExecutionMode` / `SupportedExecutionModes` / `IsSupportedExecutionMode` 拆入 `internal/usecase/agent_execution_modes.go`，将 `ToTypesAgentError` 拆入 `internal/usecase/agent_errors.go`，并将 `workflow/steps` 输入映射拆入 `orchestration_input_adapter.go`（验证命令：`go test ./internal/usecase/... ./workflow/steps ./agent/team/... -count=1`；通过标准：执行模式包装仍只委托 `agent/team`，输入映射行为不变）
- [x] Phase-2 Handler 统一：新增 `api/handlers/request_params.go` 与 `api/handlers/error_adapters.go`，并把 `apikey` / `authorization_audit` / `rag` / `tool_approval` / `tool_provider` / `tool_registry` 收口到 `BaseHandler[T]`（验证命令：`rg 'BaseHandler\[' api/handlers/*.go | wc -l && rg 'func asTypesError' api/handlers`；通过标准：service-backed handler 的 `BaseHandler` 命中数达到目标且不再保留局部 `asTypesError`）
- [x] Phase-3 Bootstrap 变薄：新增 `serve_handler_set_{health,admin,text,multimodal,workflow}_builder.go`，并用 `workflow_step_*` 拆分 `buildStepDependencies(...)` 的内部实现（验证命令：`go test ./internal/app/bootstrap/... -count=1 && wc -l internal/app/bootstrap/serve_handler_set_builder.go internal/app/bootstrap/workflow_step_dependencies_builder.go`；通过标准：唯一入口仍在，主 builder 文件显著变薄）
- [x] Phase-4 守卫与文档同步：新增 `TestServiceBackedHandlersUseBaseHandler` 与 `TestExecutionModeCatalogSingleSource`，并在 `docs/architecture/启动装配链路与组合根说明.md` 写明新的唯一归口规则（验证命令：`go test . -run 'TestServiceBackedHandlersUseBaseHandler|TestExecutionModeCatalogSingleSource' -count=1`；通过标准：新增守卫通过且架构文档能检索到规则）

## 测试策略（TDD）

- [x] 先写失败测试：为 orchestration 输入适配、request/path/query helper、service-backed handler 统一、execution mode catalog 单一来源补红灯测试（验证命令：`go test ./workflow/steps ./api/handlers/... -run 'TestBuildOrchestrationAgentInput|TestPathUintID|TestParsePositiveQueryInt' -count=1`；通过标准：新增测试覆盖关键收口点并在实现前可表达预期）
- [x] 最小实现让测试转绿：按最小改动引入 helper 文件与 thin wrapper 文件，先保证行为等价，再让新增测试与既有回归同时转绿（验证命令：`go test ./api/handlers/... ./workflow/steps ./internal/usecase/... ./internal/app/bootstrap/... -count=1`；通过标准：新增测试与受影响包测试全部为绿色）
- [x] 重构并回归验证：在测试转绿后继续拆薄 `BuildServeHandlerSet(...)` / `buildStepDependencies(...)`，删除重复样板并运行守卫回归（验证命令：`go test . -run 'TestWorkflowTeamBoundary|TestServiceBackedHandlersUseBaseHandler|TestExecutionModeCatalogSingleSource' -count=1`；通过标准：重构完成后守卫保持绿色且没有新增并行入口）

## 完成定义（DoD）

- [x] 四个官方公开入口保持不变（验证命令：`go test . -run 'TestOfficialEntrypointDocsConsistency|TestWorkflowTeamBoundary' -count=1`；通过标准：官方公开入口守卫保持绿色）
- [x] execution mode 真正实现只在 `agent/team/execution.go`，其它层只保留 thin wrapper（验证命令：`go test . -run 'TestExecutionModeCatalogSingleSource' -count=1`；通过标准：不存在新的 mode catalog 定义散点）
- [x] 除 `health.go` / `protocol.go` 外所有 service-backed handler 统一使用 `BaseHandler[T]`（验证命令：`go test . -run 'TestServiceBackedHandlersUseBaseHandler' -count=1`；通过标准：例外范围固定，其余 handler 全部收口）
- [x] handler 层不再保留局部 `asTypesError`，公共 path/query/error helper 已落地（验证命令：`rg 'func asTypesError|pathUintID|parsePositiveQueryInt|asTypesAPIError' api/handlers`；通过标准：旧局部桥接已删除，新 helper 可检索）
- [x] `BuildServeHandlerSet(...)` / `buildStepDependencies(...)` 仍是唯一入口，但内部职责已拆分到子 builder 文件（验证命令：`go test ./internal/app/bootstrap/... -count=1`；通过标准：bootstrap 包通过且新 helper 文件存在）
- [x] `RuntimeRef[T]` 已在架构文档中被说明为 usecase mutable runtime 唯一模式（验证命令：`grep -n 'RuntimeRef' docs/architecture/启动装配链路与组合根说明.md`；通过标准：文档可检索到唯一归口说明）
- [x] 新总计划、受影响包测试、架构守卫均通过（验证命令：`python3 scripts/refactor_plan_guard.py gate --target '仓库级统一复用与减冗余总计划-2026-04-26.md' --require-tdd --require-verifiable-completion && go test ./api/handlers/... ./workflow/steps ./internal/usecase/... ./internal/app/bootstrap/... ./agent/team/... ./agent/runtime/orchestration/... -count=1`；通过标准：gate 允许停止/收尾，受影响包全部为绿色）
