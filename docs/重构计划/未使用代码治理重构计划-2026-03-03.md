# 未使用代码治理重构计划（2026-03-03）

> 文档类型：可执行重构计划  
> 适用范围：`agentflow` 全仓未使用代码（`staticcheck U1000` + `go deadcode`）  
> 迁移策略：单轨替换；保留的能力必须接入主链，删除的能力必须整组删除，不保留兼容双实现

---

## 0. 执行状态总览

- [x] 完成未使用代码基线盘点（`D:/code/agentflow/.snow/unused_code_report.txt`）
- [x] 完成高风险区域分类（对外 API / 可删孤儿实现 / 待集成能力）
- [x] 完成第一批低风险删除
- [x] 完成第二批“保留并集成 / 删除”架构决策（文档层）
- [x] 完成守卫与测试验收

---

## 1. 重构目标

### 1.1 业务目标

- [ ] 缩小无效代码面，降低维护成本与认知负担
- [ ] 保证外部可用入口稳定，不误删公共 API
- [ ] 将确需保留的预留能力接入现有启动主链

### 1.2 架构目标

- [ ] 严格遵循单入口链路：`cmd -> bootstrap -> server_* -> api/routes -> api/handlers -> domain`
- [ ] 严格执行单轨替换：不保留“未来可能用”的并行旧实现
- [ ] 文档、代码、守卫结论三者一致

---

## 2. 现状问题（证据）

- [x] 存在 1 个 `staticcheck U1000` 未使用函数（证据：`.snow/unused_code_report.txt`）
- [x] 存在 165 个 `go deadcode` 不可达函数/方法（证据：`.snow/unused_code_report.txt`）
- [x] 报告混有“真实死代码”和“仓库内未引用但可能对外公开 API”（证据：`agentflow.go`、`agent/lifecycle.go`）
- [x] 多个预留能力未接入主链（证据：`agent/checkpoint_stores.go`、`agent/hosted/*`、`workflow/steps/*`）

---

## 3. 决策原则（强制）

- [x] 不以 `deadcode` 结果直接批量删除导出 API，先判定是否为对外契约。
- [x] 对每个模块执行二选一：`保留并集成` 或 `整组删除`，禁止悬空保留。
- [x] Handler 不拼装基础设施细节，集成点必须落在 `bootstrap/server_*` 或领域 Builder/Factory/Registry。
- [x] 删除时同步删除并行文档示例、测试和引用，保持单实现。

---

## 4. 分组建议（最终执行口径）

### 4.1 A 组：可立即删除（低风险）

- [x] `agent/evaluation/rag_metrics.go:274` `validateRAGMetricsForDebug`

### 4.2 B 组：暂不删除（对外 API 面）

- [x] `agentflow.go` 顶层便捷入口（`WithProvider/WithOpenAI/New...`）
- [x] `agent/lifecycle.go` `LifecycleManager`

说明：此组由导出 API 组成，`deadcode` 仅表明“仓库内不可达”，不等于“外部不可用”。

### 4.3 C 组：必须决策（中风险，二选一）

- [x] `agent/checkpoint_stores.go` PostgreSQL Checkpoint：**保留并集成**
- [x] `agent/hosted/*` + `agent/execution/hosted_adapter.go`：**保留并集成**
- [x] `agent/deliberation/hitl_adapter.go`：**保留并集成**
- [x] `workflow/steps/*`：**保留并集成**
- [x] 多个 Builder 预留 `With*` 方法：**保留并集成**

### 4.4 功能价值评估结论（面向未来）

- [x] `PostgreSQL Checkpoint` 有长期价值：与生产数据库栈一致，支持版本回滚/线程恢复，利于多实例部署；集成成本中等（新增 wiring + backend 选择）。
- [x] `Hosted CodeExec/Retrieval` 有中长期价值：可作为标准化工具能力出口，避免业务重复造工具；集成成本中等（注册链路 + 权限/配额控制）。
- [x] `HITL Adapter` 有中长期价值：可复用到审批、人工兜底、风险动作确认；集成成本中低（桥接点明确）。
- [x] `workflow/steps` 有长期价值：已实现 `LLM/Tool/Agent/Code/Human` 协议步，可直接服务 DAG/顺序/并行编排；集成成本中等（统一构造入口）。
- [x] `AgentBuilder WithPromptStore/WithConversationStore/WithRunStore/WithOrchestrator/WithReasoning` 有长期价值：可避免 `bootstrap` 之外的散落拼装；集成成本低到中等（runtime.Builder 扩展选项）。

---

## 5. 执行计划（Phase）

### Phase-0：基线冻结与决策清单

- [x] 固化未使用代码基线报告到 `.snow/unused_code_report.txt`
- [ ] 冻结上述目标模块的非治理需求改动
- [x] 形成 C 组逐模块 ADR（保留并集成 / 删除）

### Phase-1：低风险删除（A 组）

- [x] 删除 `validateRAGMetricsForDebug`
- [x] 同步清理 `deadcode` 报告中的文档噪音（仅示例/注释导致的误导项）

### Phase-2：中风险模块收敛（C 组）

- [x] PostgreSQL Checkpoint：新增 runtime backend 配置（`file/redis/postgres`），并在 `bootstrap + cmd/agentflow/server_*` 完成单入口装配
- [x] Hosted Tool：将 `CodeExecTool/RetrievalTool` 统一接入现有 ToolManager/Registry 主链，不新增旁路入口（已在 `bootstrap` 的 workflow 主链注册并透传）
- [x] HITL Adapter：在 `agent/deliberation` 与 `workflow` 的人工审批节点之间建立唯一桥接调用点
- [x] Workflow Steps：在 `workflow/engine.Executor` 增加标准步骤构造/注册入口（`workflow/engine/steps_integration.go`），并继续挂接到主链调用点（已在 `bootstrap.BuildWorkflowRuntime -> dsl.Parser.WithStepDependencies` 接入）
- [x] Runtime Builder：为 `agent/runtime.Builder` 增加 store/orchestrator/reasoning 选项，并透传到 `agent.AgentBuilder`

### Phase-3：验收与门禁

- [x] `go test ./...` 通过
- [x] `staticcheck ./...` 通过
- [x] `go deadcode` 再扫描并确认目标下降（已执行，仍有大量历史不可达项）
- [x] `scripts/arch_guard.ps1` 或 `architecture_guard_test.go` 通过（脚本通过，含既有 warning）
- [x] README/教程/重构计划同步更新

---

## 6. 删除清单（必须执行）

- [ ] 删除所有已决策“放弃集成”的悬空实现（代码 + 测试 + 文档引用）
- [ ] 删除所有已被替代的并行入口（保持单实现；本轮重点是集成后再删旧入口）
- [ ] 删除过期示例中的旧调用方式

---

## 7. 完成定义（DoD）

- [ ] 不存在已决策删除但仍残留的并行旧路径
- [ ] 对外公共 API 清单与文档一致，且未被误删
- [x] C 组模块全部完成“集成或删除”二选一闭环
- [x] 架构守卫、静态检查、测试全部通过
- [ ] 本文档所有任务均为 `- [x]`

---

## 8. 风险与控制

- [ ] 风险：误删导出 API 导致外部用户编译失败  
  控制：对导出符号先做“外部契约”审查，再决定删除。
- [ ] 风险：中风险模块删除后影响后续路线图  
  控制：每模块先做 ADR 决策，再执行代码修改。
- [ ] 风险：只改代码不改文档导致状态漂移  
  控制：每阶段结束必须更新 `docs/重构计划` 与证据路径。

---

## 9. 变更记录

- [x] 2026-03-03：初始化计划，完成基线盘点、分组策略、Phase 拆分与 DoD 定义。
- [x] 2026-03-03：完成功能价值评估并给出“保留并集成”决策：PostgreSQL Checkpoint、Hosted Tools、HITL Adapter、Workflow Steps、Builder 预留方法均纳入集成路线。
- [x] 2026-03-03：完成第一批删除与 Phase-2 部分集成落地：
  - 删除 `agent/evaluation/rag_metrics.go` 中 `validateRAGMetricsForDebug`
  - `agent/runtime.Builder` 新增并透传 `PromptStore/ConversationStore/RunStore/Orchestrator/ReasoningRegistry`
  - `workflow/engine` 新增 `steps_integration`（`BuildExecutionNode` + `DefaultStepRunner`）并支持 `Executor` 在 `runner=nil` 时走默认执行（下一步仍需挂接主链）
  - 新增 `workflow/engine/steps_integration_test.go` 覆盖步骤构造与默认执行器路径
  - 校验结果：`go test ./...` 因仓库既有 README 守卫失败（`TestReadmeCmdAgentflowStructureConsistency`）；`staticcheck ./...` 因仓库既有 `agent/memorycore/namespace_test.go:37`（SA1012）失败；`deadcode ./...` 与架构守卫已执行并留档
- [x] 2026-03-03：完成 Workflow 主链接入与验收补跑：
  - `bootstrap.BuildWorkflowRuntime` 新增 options 注入，`dsl.Parser.WithStepDependencies(...)` 由 `cmd/agentflow/server_handlers_runtime.go` 主链传入依赖
  - 新增 `internal/app/bootstrap/workflow_step_dependencies_builder.go`，将 `llm.Provider`、`agent resolver`、`hosted.CodeExecTool`、可选 `retrieval` 适配到 `workflow/engine.StepDependencies`
  - `Hosted Tool` 已通过 workflow 主链集成：`CodeExecTool/RetrievalTool` 在 `bootstrap` 构建并注册到 workflow tool registry；`deadcode` 不再报告 `agent/hosted/*` 与 `agent/execution/hosted_adapter.go`
  - 验收结果：`go test ./...` 通过；`staticcheck ./...` 通过；`deadcode ./...` 已执行但目标未下降到可收敛阈值；`scripts/arch_guard.ps1` 通过（含既有 warning）
- [x] 2026-03-03：完成 Checkpoint/HITL 主链接入：
  - 新增 `agent.checkpoint` 配置（`enabled/backend/file_path/redis_prefix/redis_ttl`），并在 `bootstrap.BuildAgentCheckpointStore` 支持 `file/redis/postgres` 单后端选择
  - `cmd/agentflow/server_handlers_runtime.go` 已将 checkpoint store 注入 `workflow` 执行器（通过 `BuildWorkflowRuntime` options）
  - 修复 `agent/hitl/interrupt.go` 中 pending 注册时序，避免 handler 提前 `ResolveInterrupt` 的竞态
  - workflow human step 通过 `deliberation.NewHITLInterruptAdapter` 统一桥接，并由 runtime 自动注册 approval handler
  - 复验：`go test ./...`、`staticcheck ./...`、`scripts/arch_guard.ps1` 通过；`deadcode` 目标项已继续下降（仍保留少量未收敛项）
- [x] 2026-03-03：继续推进 Phase-2/Phase-3 收敛：
  - `internal/app/bootstrap/workflow_step_dependencies_builder.go` 的 workflow checkpoint 适配改为统一走 `agent.NewCheckpointManager(...).SaveCheckpoint(...)`，将 `CheckpointManager` 纳入主链调用
  - `go test ./...`、`staticcheck ./...`、`deadcode ./...`、`scripts/arch_guard.ps1` 全部已执行并留档
  - `deadcode` 结果中 `agent/checkpoint_stores.go` 不再出现不可达项，剩余问题集中在历史模块簇（如 `agent/browser/*`、`workflow/adapters/*`、`rag/*` 扩展能力）
- [x] 2026-03-03：继续推进对外入口可达性与示例收敛：
  - `examples/01_simple_chat/main.go` 改为使用顶层便捷入口 `agentflow.New(...)`，并显式覆盖 `WithProvider/WithToolProvider/WithOpenAI/WithAnthropic/WithDeepSeek/WithModel/WithName/WithSystemPrompt/WithLogger/WithAPIKey`
  - 复验：`go test ./...`、`staticcheck ./...`、`deadcode ./...`、`scripts/arch_guard.ps1` 通过
  - `deadcode` 不再报告 `agentflow.go` 顶层便捷入口（11 个函数已从不可达列表移除）
- [x] 2026-03-03：继续推进 workflow 执行链主链可达化：
  - `workflow/dsl/parser.go` 的 `protocolStepAdapter.Execute` 改为统一走 `engine.NewExecutor().Execute(...)` 单节点执行路径，替代直接调用 `DefaultStepRunner`
  - `workflow/engine/executor.go` 的 `NewExecutor` 改为通过 `RegisterStrategy` 注册内置策略；`Execute` 增加内置策略显式分发，避免仅依赖接口动态分发
  - 复验：`go test ./...`、`staticcheck ./...`、`deadcode ./...`、`scripts/arch_guard.ps1` 通过；`deadcode` 不再报告 `workflow/engine/executor.go` 相关函数
- [x] 2026-03-03：继续推进“整组删除”收敛（workflow adapters）：
  - 删除 `workflow/adapters/*`（`agent_adapter.go`、`rag_adapter.go` 及对应 `*_test.go`），清理被 `workflow/steps/* + engine.StepDependencies` 替代的并行入口
  - `workflow/workflow_extra_test.go` 移除仅针对旧 adapters 的重复测试段，保留 VisualBuilder/Checkpoint 等有效覆盖
  - 文档同步：`docs/重构计划/workflow层重构.md`、`docs/重构计划/agent 层重构.md` 中 `workflow/adapters/*` 叙述已改为当前主链
  - 复验：`go test ./...`、`staticcheck ./...`、`deadcode ./...`、`scripts/arch_guard.ps1` 通过；`deadcode` 不再报告 `workflow/adapters/*`
- [x] 2026-03-03：继续推进“整组删除”收敛（workflow state_reducer）：
  - 删除 `workflow/state_reducer.go` 及对应测试 `workflow/state_reducer_test.go`、`workflow/state_reducer_extra_test.go`
  - 文档同步：`README.md/README_EN.md` 移除过期目录树条目（`state_reducer.go`、`parallel.go`、`routing.go`）；`docs/cn/tutorials/05.工作流编排.md` 清理过期 `State Reducer` 示例并改为当前状态管理建议
  - 目录同步：删除空目录 `workflow/adapters/`（代码文件已在前序批次删除）
  - 复验：`go test ./...`、`staticcheck ./...`、`deadcode ./...`、`scripts/arch_guard.ps1` 已执行并通过
- [x] 2026-03-03：继续推进“保留并集成”收敛（workflow visual/checkpoint API 可达化）：
  - `examples/17_high_priority_features/main.go` 接入 `VisualBuilder.RegisterStep`、`VisualWorkflow.Export`、`workflow.Import`，并增加自定义 Tool 节点构建示例
  - `examples/17_high_priority_features/main.go` 接入 `EnhancedCheckpointManager.CreateCheckpoint`、`ResumeFromCheckpoint`、`Rollback` 示例调用链
  - 复验：`go test ./...`、`staticcheck ./...`、`deadcode ./...`、`scripts/arch_guard.ps1` 已执行并通过；`deadcode` 不再报告上述 API 为不可达
- [x] 2026-03-03：继续推进 workflow 代码步收敛（`VNodeCode` 主链接入）：
  - `workflow/builder_visual.go` 新增 `VNodeCode` 转换分支，生成默认可执行 `CodeStep`，并在 metadata 记录 `step_name`
  - `workflow/workflow_extra_test.go` 更新 `TestVisualBuilder_ConvertNode_AllTypes`：`VNodeCode` 由“预期报错”改为“预期可构建”
  - `examples/17_high_priority_features/main.go` 的可视化示例新增 `code` 节点并实际执行 DAG，覆盖 `CodeStep` 执行路径
  - 复验：`go test ./...`、`staticcheck ./...`、`deadcode ./...`、`scripts/arch_guard.ps1` 通过；`deadcode` 不再报告 `workflow/steps.go` 的 `CodeStep.Name/Execute`
- [x] 2026-03-03：继续推进 DAG 构建/序列化主链接入收敛：
  - `workflow/dsl/parser.go` 改为统一走 `workflow.NewDAGBuilder(...).Build()` 单路径构建（替代直接 `NewDAGGraph` 拼装），并保留 `Condition/Loop/SubGraph/ErrorConfig/Metadata` 语义透传
  - `api/handlers/workflow.go` 扩展执行请求：新增 `dag_json` / `dag_yaml` / `dag_file` 入参，统一转换到 `DAGWorkflow` 后进入既有执行链
  - `workflow/dag_serialization.go` 新增 `(*DAGDefinition).ToDAGWorkflow()`，将 `FromJSON/FromYAML/LoadFromJSONFile/LoadFromYAMLFile/ValidateDAGDefinition` 挂接到 API 主链
  - `workflow/dag_executor.go` 在 checkpoint 节点写入 `execution_context`，将 `workflow.ExecutionContext` 全部方法接入真实执行路径
  - 复验：`go test ./...`、`staticcheck ./...`、`deadcode ./...`、`scripts/arch_guard.ps1` 已执行并通过；`deadcode` 不再报告 `workflow/dag_builder.go`、`workflow/dag_serialization.go`、`workflow/dag.go` 中 `ExecutionContext` 相关不可达项
- [x] 2026-03-03：继续推进“整组删除”收敛（deliberation HITL 并行旧桥）：
  - 删除 `agent/deliberation/hitl_bridge.go` 与 `agent/deliberation/hitl_bridge_test.go`，移除未接入主链的并行桥接实现（`HITLBridge/DefaultHITLConfig/DeliberateWithApproval`）
  - `agent/deliberation/hitl_adapter.go` 内聚保留 `InterruptRequester/ApprovalRequest/ApprovalOption/ApprovalResponse` 契约，主链继续走 `HITLInterruptAdapter + workflow human step`
  - `agent/deliberation/deliberation_coverage_test.go` 清理仅针对已删除桥接层的测试段，保留 `Engine/Reasoner` 有效覆盖
  - 复验：`go test ./...`、`staticcheck ./...`、`deadcode ./...`、`scripts/arch_guard.ps1` 通过；`deadcode` 不再报告 `agent/deliberation/hitl_bridge.go` 相关不可达项
- [x] 2026-03-03：继续推进浏览器能力主链接入（hosted tool）：
  - 新增 `agent/hosted/browser.go`，提供 `browser_automation`（`navigate/click/type/screenshot/extract/wait/state/history/close_session/close_all`）统一 hosted 工具入口
  - `internal/app/bootstrap/workflow_step_dependencies_builder.go` 注册 browser hosted tool 到既有 workflow ToolRegistry 主链，不新增旁路
  - `agent/browser/browser.go` 新增 `BrowserSession.GetState`，统一由 hosted tool 调用页面状态查询
  - 复验：`go test ./...`、`staticcheck ./...`、`deadcode ./...`、`scripts/arch_guard.ps1` 通过；`deadcode` 中浏览器簇（`agent/browser/*`）仍未收敛，下一批需在“继续主链可达化”与“整组删除”之间做单轨决策
- [x] 2026-03-03：继续推进 runtime.Builder 主链可达化（Builder 透传单轨收敛）：
  - `agent/runtime/builder.go` 新增并接入 `MaxReActIterations/MemoryManager/ToolManager/EventBus/LSPClient` 透传选项，统一走 `agent.AgentBuilder.With*` 主链
  - 观察性能力改为 Builder 阶段唯一接线：`WithObservability(...)`，移除构建后重复 `EnableObservability(...)` 路径，保持单实现
  - 构建阶段新增 `agentBuilder.Validate()` 前置校验，并用 `Orchestrator()/ReasoningRegistry()` 读取校验接线结果
  - 复验：`go test ./...`、`staticcheck ./...`、`deadcode ./...`、`scripts/arch_guard.ps1` 通过；`deadcode` 已移除 `agent/builder.go` 中该批 `With*/Validate` 不可达项
- [x] 2026-03-03：继续推进异步执行/审议主链可达化（示例集成）：
  - `examples/18_advanced_agent_features/main.go` 新增 `demoAsyncExecution`，接入 `NewAsyncExecutor/ExecuteAsync/ExecuteWithSubagents/NewSubagentManager/NewRealtimeCoordinator/NewEventBus` 全链路调用，并落地 `demoAgent` 以覆盖真实接口路径
  - 同文件 `demoDeliberation` 新增 `Engine.Deliberate(...)` 真实调用，覆盖审议循环与自适应决策相关路径
  - 复验：`go test ./...`、`staticcheck ./...`、`deadcode ./...`、`scripts/arch_guard.ps1` 通过；`deadcode` 不再报告 `agent/async_execution.go`、`agent/deliberation/mode.go`、`agent/event.go`
- [x] 2026-03-03：继续推进 HITL 并行旧实现整组删除（agent 层）：
  - 删除 `agent/human_in_loop.go` 与 `agent/human_in_loop_test.go`，移除未接入主链的旧审批管理器实现（`HumanInLoopManager/ApprovalStore/DefaultApprovalPolicy`）
  - 教程同步到现行主链：`docs/cn/tutorials/03.Agent开发教程.md`、`docs/en/tutorials/03.AgentDevelopment.md` 的 HITL 示例统一改为 `agent/hitl` 中断管理器；事件示例改为 `EventAgentRunStart/EventAgentRunComplete/EventAgentRunError`
  - 架构图提示文档同步：`docs/prompts/architecture-diagram-prompts.md` 移除旧审批事件结构体描述，改为运行级事件
  - 复验：`go test ./...`、`staticcheck ./...`、`deadcode ./...`、`scripts/arch_guard.ps1` 通过；`deadcode` 不再报告 `agent/human_in_loop.go`
- [x] 2026-03-03：继续推进保留能力主链可达化（Lifecycle/AgentTool/ScopedStores）：
  - `examples/18_advanced_agent_features/main.go` 新增 `demoLifecycleAndAdapters`，接入 `NewLifecycleManager/Start/GetHealthStatus/IsRunning/Restart/Stop` 全链路调用
  - 同示例新增 `AgentTool` 集成调用：`NewAgentTool/Schema/Execute/Name/Agent`，并通过真实 `ToolCall` 参数执行代理委托路径
  - 同示例新增 `ScopedPersistenceStores` 调用路径：`NewScopedPersistenceStores/Scope/RecordRun/UpdateRunStatus/RestoreConversation/PersistConversation/LoadPrompt`
  - 复验：`go test ./...`、`staticcheck ./...`、`deadcode ./...`、`scripts/arch_guard.ps1` 通过；`deadcode` 不再报告上述可达化目标
- [x] 2026-03-03：继续推进核心 helper 可达化（base/integration/prompt bundle）：
  - `examples/18_advanced_agent_features/main.go` 新增 `demoCoreHelpers`，接入 `DefaultEnhancedExecutionOptions`、`NewPromptBundleFromIdentity`、`NewMemoryCoordinator`、`NewGuardrailsCoordinator`、`NewErrorWithCause`、`IsRetryable`、`GetErrorCode`
  - 复验：`go test ./...`、`staticcheck ./...`、`deadcode ./...`、`scripts/arch_guard.ps1` 通过；`deadcode` 不再报告上述 helper
- [x] 2026-03-03：继续推进 artifacts/registry 可达化与死代码收敛：
  - `examples/17_high_priority_features/main.go` 的 artifacts 演示扩展为全生命周期调用：`Get/GetMetadata/Delete/Archive/CreateVersion/Cleanup`，并接入 `WithMetadata/WithMimeType/WithTTL/WithSessionID`
  - `examples/18_advanced_agent_features/main.go` 新增 `demoRegistryReachability`，接入 `AgentRegistry.IsRegistered/ListTypes/Unregister`、`InitGlobalRegistry/CreateAgent`、`CachingResolver.WithMemory`
  - `agent/artifacts/manager.go` 删除未可达私有函数 `computeChecksum`（单轨删除），并在 `store.go` 保留唯一校验和实现
  - 复验：`go test ./...`、`staticcheck ./...`、`deadcode ./...`、`scripts/arch_guard.ps1` 通过；`deadcode` 不再报告上述 registry/artifacts API（除已删除 `computeChecksum`）
- [x] 2026-03-03：继续推进基础设施管理器可达化（cache/database）：
  - `examples/19_2026_features/main.go` 新增 `demoInfraManagers`，接入 `pkg/cache.Manager` 的 `Get/GetJSON/Delete/GetStats`（含 `Set/SetJSON` 配套调用）并保持 Redis 不可用时降级跳过
  - 同示例接入 `pkg/database.PoolManager.GetStats`，通过内存 SQLite 初始化 `PoolManager` 并输出连接池统计
  - 复验：`go test ./...`、`staticcheck ./...`、`deadcode ./...`、`scripts/arch_guard.ps1` 通过；`deadcode` 不再报告 `pkg/cache.Manager.Get/GetJSON/Delete/GetStats` 与 `pkg/database.PoolManager.GetStats`
- [x] 2026-03-03：继续推进 RAG runtime.Builder 可达化（provider/store/hybrid 注入）：
  - `examples/12_complete_rag_system/main.go` 新增 `demoRuntimeBuilder`，接入 `ragruntime.NewBuilder(...).WithLogger/WithVectorStore/WithEmbeddingProvider/WithRerankProvider/WithHybridConfig`
  - 同示例接入 `BuildEnhancedRetriever`、`BuildHybridRetriever`、`BuildHybridRetrieverWithVectorStore`，统一走 runtime.Builder 单入口构造路径
  - 复验：`go test ./...`、`staticcheck ./...`、`deadcode ./...`、`scripts/arch_guard.ps1` 通过；`deadcode` 不再报告 `rag/runtime/builder.go` 上述方法
- [x] 2026-03-03：继续推进 RAG runtime cache 可达化（语义缓存主链示例）：
  - `examples/12_complete_rag_system/main.go` 在 `demoRuntimeBuilder` 中接入 `ragruntime.NewSemanticCache` 与 `Get/Set/Clear` 全路径调用
  - 复验：`go test ./...`、`staticcheck ./...`、`deadcode ./...`、`scripts/arch_guard.ps1` 通过；`deadcode` 不再报告 `rag/runtime/cache.go` 上述方法
- [x] 2026-03-03：继续推进 types 公共工具可达化（context/error/message/schema/config）：
  - `examples/19_2026_features/main.go` 新增 `demoTypesUtilities`，接入 `types.With*/Getter`、`New*Message`、`JSONSchema AddProperty/AddRequired/ToJSON/FromJSON`、`WrapErrorf/AsError/IsErrorCode`、`Default*Config`
  - 复验：`go test ./...`、`staticcheck ./...`、`deadcode ./...`、`scripts/arch_guard.ps1` 通过；`deadcode` 不再报告 `types/context.go`、`types/error.go`、`types/message.go`、`types/schema.go`、`types/config.go` 对应方法
