# 多模态功能实现报告

> 文档定位：当前状态报告 + 历史里程碑说明  
> 更新时间：2026-04-21

本文档不再保留大段“首轮实现完成时的过程性流水账”，而是压缩为：

1. **当前状态报告**：今天这个仓库里，多模态能力的覆盖范围、真实入口、推荐文档链路。  
2. **历史里程碑说明**：2026-02 那轮接口覆盖工作为什么重要、留下了哪些资产。

---

## 一、当前状态报告

### 1.1 现在应该先看哪些文档

| 文档 | 用途 |
|---|---|
| `docs/cn/guides/近12个月主流多模态模型总表.md` | 看近 12 个月主流模型家族 |
| `docs/模型与媒体端点参考.md` | 看 provider `/models`、chat / image / video / speech 总览 |
| `docs/多模态能力端点参考.md` | 看当前代码已实现能力矩阵与端点 |
| `docs/视频与图像厂商及端点说明.md` | 看图像 / 视频厂商接入、配置、共享 key |
| 本文档 | 看当前状态摘要 + 历史背景 |

### 1.2 当前真实架构

- OpenAI / Anthropic Claude / Gemini：原生 provider 主路径
- compat 厂商的 chat 主链：统一走 `llm/providers/vendor.NewChatProviderFromConfig(...)`
- `llm/providers/<vendor>/multimodal.go`：表示厂商能力实现位置，不再表示公共 chat 构造入口
- `deepseek / kimi / llama / hunyuan` 的独立 compat chat 目录已删除

### 1.3 当前覆盖面（按代码事实）

| 能力 | 当前覆盖面 |
|---|---|
| 图像生成 | OpenAI、Gemini、Qwen、GLM、Grok、Doubao |
| 视频生成 | Gemini、GLM |
| 音频生成 / TTS | OpenAI、Gemini、Qwen、Doubao、MiniMax |
| 音频转录 / STT | OpenAI、Mistral |
| Embedding | OpenAI、Gemini、Qwen、GLM、Grok、Doubao、Mistral |
| 微调 | OpenAI |

### 1.4 当前维护建议

1. **把“当前代码已实现”与“官方最近 12 个月主流模型”分开。**
2. **多模态说明统一收口到 `docs/`，不要再新增散落在 provider 目录的主文档。**
3. **当仓库结构变了，优先修正文档基线，而不是继续堆历史过程描述。**

---

## 二、历史里程碑说明（2026-02）

### 2.1 这轮历史工作解决了什么问题

2026-02 的那轮多模态工作，核心不是“把所有厂商能力都做到最完整”，而是：

- 先把统一接口补齐
- 先把请求 / 响应类型收口
- 先把 provider 接口覆盖打通
- 先把能支持的能力实现起来，不能支持的统一返回标准错误

这是当前多模态能力能继续演进的基础。

### 2.2 历史遗产（仍然有价值）

| 历史产物 | 现在的价值 |
|---|---|
| `llm/multimodal.go` | 仍是多模态契约核心 |
| `llm/providers/multimodal_helpers.go` | 仍是 compat helper 基线 |
| provider `multimodal.go` 文件 | 仍是各厂商能力实现主位置 |
| 首轮文档 | 现在可作为历史背景，但不应直接当成当前基线 |

### 2.3 为什么要压缩旧描述

旧报告的问题不是“错”，而是：

- 太像阶段性庆功稿，不像当前维护文档
- 容易把“接口覆盖完成”和“厂商能力完整支持”混为一谈
- 容易把旧模型名、旧路径、旧入口继续带到现在

所以现在把它收缩成“历史里程碑说明”，只保留必要背景。

---

## 三、当前和历史怎么配合使用

- **看今天怎么做**：优先看“当前状态报告”
- **看当年为什么这么设计**：再看“历史里程碑说明”
- **看具体能力与端点**：回到 `docs/多模态能力端点参考.md`
- **看厂商和模型总览**：回到 `docs/模型与媒体端点参考.md` 与 `docs/cn/guides/近12个月主流多模态模型总表.md`
