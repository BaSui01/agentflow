# 🔍 Bug 审计报告：最近改动全方位代码审核

> 📅 创建时间：2026-03-02 00:59:09
> 🔖 审计维度：架构设计 / 代码质量 / 安全性 / 性能 / 可维护性
> 📊 总体评分：7.6/10

---

## 📊 总览仪表盘

| 维度 | 评分 | P0 | P1 | P2 |
|------|------|----|----|----|
| 🚨 错误捕获 | 8/10 | 0 | 0 | 1 |
| 🛡️ 输入准入 | 8/10 | 0 | 1 | 0 |
| 🧩 状态机 | 9/10 | 0 | 0 | 0 |
| 🛰️ 观测链路 | 8/10 | 1 | 0 | 0 |
| 🔗 前后端契约 | 7/10 | 0 | 1 | 0 |
| ⚡ 性能资源 | 6/10 | 0 | 1 | 0 |
| 🔒 安全漏洞 | 8/10 | 0 | 0 | 1 |
| 🔄 并发事务 | 8/10 | 0 | 0 | 0 |
| 📐 代码分层 | 8/10 | 0 | 0 | 1 |
| **总计** | **7.6/10** | **1** | **3** | **3** |

---

## 📋 Bug 清单

### 🚨 严重问题 (Critical)

| 编号 | 状态 | 文件:行号 | 问题描述 | 严重程度 | 修复建议 |
|------|------|-----------|----------|----------|----------|
| C-001 | [ ] | `pkg/middleware/middleware.go:77` | `RequestLogger` 包装器未实现 `http.Flusher`，会让下游 SSE handler 的 `w.(http.Flusher)` 断言失败。 | P0 | 在 `responseWriter` 上补全 `Flush()` 并透传到底层 writer；同时给 tracing writer 也补全 `Flush()`。 |
| C-002 | [ ] | `pkg/middleware/middleware.go:155` | `tracingResponseWriter` 未实现 `http.Flusher`，在链式包装中继续放大 SSE 不可用风险。 | P0 | 为 `tracingResponseWriter` 增加 `Flush()`（必要时 `ReadFrom`/`Unwrap`）并添加集成测试覆盖 `/stream` 端点。 |

### ⚠️ 警告 (Warning)

| 编号 | 状态 | 文件:行号 | 问题描述 | 严重程度 | 修复建议 |
|------|------|-----------|----------|----------|----------|
| W-001 | [ ] | `config/api.go:630` | 配置 API 限流器使用 `map[string]*rate.Limiter` 且无过期清理，面对大量源 IP 会持续增长。 | P1 | 采用与全局限流同样的周期清理策略（lastSeen + TTL）或使用带过期缓存。 |
| W-002 | [ ] | `cmd/agentflow/server_http.go:249` | API Key 鉴权固定 `allowQueryAPIKey=false`，`pkg/middleware.APIKeyAuth` 入参 `allowQueryAPIKey` 被忽略，存在契约变更风险。 | P1 | 若确定禁用 query key，应删除该参数并同步 OpenAPI/迁移说明；否则恢复配置驱动。 |
| W-003 | [ ] | `cmd/agentflow/server_http.go:269` | fail-closed 返回 `503 AUTH_MISCONFIGURED`，安全上正确，但会改变旧部署行为（曾可匿名访问）。 | P1 | 在升级说明中明确“默认拒绝”变更，并提供启动时强校验/健康探针提示。 |

### 💡 优化建议 (Suggestion)

| 编号 | 状态 | 文件:行号 | 问题描述 | 严重程度 | 修复建议 |
|------|------|-----------|----------|----------|----------|
| S-001 | [ ] | `api/routes/routes.go:49` | 路由 method 分派仍在匿名函数中，路径/方法元数据分散。 | P2 | 采用 Go 1.22+ pattern（如 `"GET /api/v1/..."`）或路由表统一声明。 |
| S-002 | [ ] | `pkg/middleware/middleware.go:329` | 限流键基于 `RemoteAddr`，反向代理部署下可能全部聚合到代理 IP。 | P2 | 引入可信代理配置，按 `X-Forwarded-For`/`X-Real-IP` 解析真实来源。 |
| S-003 | [ ] | `api/types.go:385` | A2AResponse 的 `Status` 注释已收敛，但建议补充枚举验证/文档示例一致性校验。 | P2 | 在 OpenAPI schema 增加 enum: `pending|processing|completed|failed`。 |

---

## ✅ 做得好的地方

- `cmd/agentflow/server_http.go:230` 认证策略改为 fail-closed，安全默认值更合理。
- `pkg/middleware/middleware.go:314` 与 `:572` 限流清理从后台 goroutine 改为请求内周期清理，减少生命周期泄漏风险。
- `agent/*` facade + `agent/*core` 下沉后，分层边界更清晰，删除了旧双实现。
- `config/api.go:604` 禁用 query string API key，避免日志/历史泄露风险。

---

## 📚 参考资料

- golang-jwt/jwt parser validation options（issuer/audience/valid methods/exp required）: https://context7.com/golang-jwt/jwt/llms.txt
- Go net/http ResponseWriter optional interfaces（Hijacker/Flusher 透传要求）: https://go.dev/doc/devel/pre_go1

---

## 📝 后续

可使用 `/bug-fix` 技能按编号逐项修复并归档。
