# 🔍 Bug 审计报告：Embedding 入口出口统一性审计

> 📅 创建时间：2026-03-01 20:45:38
> 🔖 审计维度：📐 代码分层一致性、🛡️ 输入准入统一性
> 📊 总体评分：6/10

---

## 📊 总览仪表盘

| 维度 | 评分 | P0 | P1 | P2 |
|------|------|----|----|-----|
| 🚨 错误捕获 | 8/10 | 0 个 | 0 个 | 1 个 |
| 🛡️ 输入准入 | 5/10 | 0 个 | 1 个 | 0 个 |
| 🧩 状态机 | 9/10 | 0 个 | 0 个 | 0 个 |
| 🛰️ 观测链路 | 9/10 | 0 个 | 0 个 | 0 个 |
| 🔗 前后端契约 | 9/10 | 0 个 | 0 个 | 0 个 |
| ⚡ 性能资源 | 9/10 | 0 个 | 0 个 | 0 个 |
| 🔒 安全漏洞 | 9/10 | 0 个 | 0 个 | 0 个 |
| 🔄 并发事务 | 9/10 | 0 个 | 0 个 | 0 个 |
| 📐 代码分层 | 5/10 | 0 个 | 2 个 | 0 个 |
| **总计** | **6/10** | **0 个** | **3 个** | **1 个** |

---

## 📋 Bug 清单

### 🛡️ 维度二：输入准入统一性

| 编号 | 状态 | 文件:行号 | 问题描述 | 严重程度 | 修复建议 |
|------|------|-----------|----------|----------|----------|
| V-001 | [ ] | `llm/embedding/gemini.go:111`、`llm/embedding/openai.go:50`、`llm/embedding/cohere.go:50`、`llm/embedding/voyage.go:46`、`llm/embedding/jina.go:49` | 只有 Gemini Provider 显式校验 `len(req.Input)==0`，其余 Provider 缺少统一输入准入，导致行为不一致（有的依赖上游 4xx，有的可能本地 panic/空结果）。 | P1 | 将空输入校验统一下沉到 `BaseProvider` 公共入口（或所有 `Embed` 首行统一调用同一校验函数）。禁止多实现并存，收敛为单一准入策略。 |

### 📐 维度九：代码分层一致性

| 编号 | 状态 | 文件:行号 | 问题描述 | 严重程度 | 修复建议 |
|------|------|-----------|----------|----------|----------|
| L-001 | [ ] | `rag/factory.go:76`、`cmd/agentflow/server.go:559` | 已存在 `rag.NewEmbeddingProviderFromConfig` 作为统一构造入口，但 `Server.initRAGHandler` 未复用该入口，直接 `embedding.NewOpenAIProvider`，形成“并行入口”。 | P1 | 让 `initRAGHandler` 调用 `rag.NewEmbeddingProviderFromConfig`；删除直连构造路径，保留唯一入口。 |
| L-002 | [ ] | `rag/provider_integration.go:157`、`rag/provider_integration.go:169`、`rag/provider_integration.go:185`、`rag/provider_integration.go:201` | `rag/provider_integration.go` 中保留了多组 `NewXxxRetriever/NewXxxProvider` 直连构造捷径，弱化统一工厂边界，增加维护分叉。 | P1 | 以 `rag/factory.go` 为唯一构造中心；将捷径函数重定向到统一工厂，或删除重复入口。 |

### 🚨 维度一：错误捕获统一性

| 编号 | 状态 | 文件:行号 | 问题描述 | 严重程度 | 修复建议 |
|------|------|-----------|----------|----------|----------|
| E-001 | [ ] | `llm/embedding/openai.go:72`、`llm/embedding/cohere.go:84`、`llm/embedding/voyage.go:71`、`llm/embedding/jina.go:87`、`llm/embedding/gemini.go:147` | Provider 反序列化错误返回风格不一致：有的直接 `return err`，有的包装上下文信息（Gemini）。排障信息颗粒度不一致。 | P2 | 统一为 `fmt.Errorf("failed to decode <provider> response: %w", err)` 风格。 |

---

## 💡 系统性改进方案

### 短期修复（直接修复具体问题）
- [ ] 在 embedding 层增加统一请求校验函数并接入全部 Provider。
- [ ] 让 `cmd/agentflow/server.go` 的 RAG 初始化走 `rag.NewEmbeddingProviderFromConfig`。
- [ ] 统一各 Provider 的 JSON 反序列化错误包装。

### 中期防护（自动化检查机制）
- [ ] 增加测试：遍历所有 embedding provider，空输入应返回同类型 `llm.ErrInvalidRequest`。
- [ ] 增加测试：RAG 初始化路径不应直接调用 `embedding.NewOpenAIProvider`。

### 长期治理（架构层面提升）
- [ ] 在 `llm/embedding` 新增包内 `factory.go`（或 registry），作为该子域唯一入口；上层只依赖该入口。
- [ ] 将“统一入口”写入贡献规范，新增 provider 时必须在统一工厂注册。

---

## 📝 修复记录

| 日期 | 编号 | 修复人/工具 | 修复说明 | 关联 commit |
|------|------|-------------|----------|-------------|
| 2026-03-01 | V-001/L-001/L-002/E-001 | Codex bug-analysis | 完成审计，待修复 | |
