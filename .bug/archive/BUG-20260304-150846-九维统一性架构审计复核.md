# 🔍 Bug 审计报告：九维统一性架构审计复核

> 📅 创建时间：2026-03-04 15:08:46
> 🔖 审计维度：错误捕获、输入准入、状态机、观测链路、前后端契约、性能资源、安全漏洞、并发事务、代码分层
> 📊 总体评分：6.1/10

---

## 📊 总览仪表盘

| 维度 | 评分 | P0 | P1 | P2 |
|------|------|----|----|-----|
| 🚨 错误捕获 | 7/10 | 0 个 | 1 个 | 0 个 |
| 🛡️ 输入准入 | 5/10 | 0 个 | 2 个 | 0 个 |
| 🧩 状态机 | 6/10 | 0 个 | 0 个 | 2 个 |
| 🛰️ 观测链路 | 6/10 | 0 个 | 1 个 | 1 个 |
| 🔗 前后端契约 | 5/10 | 0 个 | 1 个 | 1 个 |
| ⚡ 性能资源 | 6/10 | 0 个 | 1 个 | 0 个 |
| 🔒 安全漏洞 | 4/10 | 1 个 | 1 个 | 0 个 |
| 🔄 并发事务 | 6/10 | 0 个 | 1 个 | 0 个 |
| 📐 代码分层 | 7/10 | 0 个 | 0 个 | 1 个 |
| **总计** | **6.1/10** | **1 个** | **8 个** | **5 个** |

---

## 📋 Bug 清单

### 🚨 维度一：错误捕获统一性

| 编号 | 状态 | 文件:行号 | 问题描述 | 严重程度 | 修复建议 |
|------|------|-----------|----------|----------|----------|
| E-001 | [x] | `llm/runtime/router/apikey_pool.go:51` | `NewAPIKeyPool` 在策略为空时直接 `panic`，可被配置错误触发为进程级失败。 | P1 | 改为 `(*APIKeyPool, error)` 并在调用方统一映射错误。 |

### 🛡️ 维度二：输入准入统一性

| 编号 | 状态 | 文件:行号 | 问题描述 | 严重程度 | 修复建议 |
|------|------|-----------|----------|----------|----------|
| V-001 | [x] | `agent/discovery/protocol.go:399` | `handleAnnounce` 直接 `io.ReadAll(r.Body)`，未设置请求体上限。 | P1 | 使用 `http.MaxBytesReader` + `413` 响应。 |
| V-002 | [x] | `api/routes/routes.go:40` | `/api/v1/agents/execute` 使用 path-only 注册，`HandleExecuteAgent` 未显式 `r.Method` 校验，方法准入不统一。 | P1 | 改为 method-aware 路由（`POST /...`）或在 handler 首行强制校验。 |

### 🧩 维度三：状态机统一性

| 编号 | 状态 | 文件:行号 | 问题描述 | 严重程度 | 修复建议 |
|------|------|-----------|----------|----------|----------|
| S-001 | [x] | `agent/discovery/protocol.go:114` | `done` 通道只在构造时创建，`Stop()` 关闭后 `Start()` 不重建，重启后监听流程可直接退出。 | P2 | `Start()` 重建 `done/closeOnce` 生命周期对象。 |
| S-002 | [x] | `agent/reasoning/plan_execute.go:112` | PlanExecute 状态值大量硬编码字符串（`completed/failed/running/...`），缺少统一常量约束。 | P2 | 抽取状态常量/枚举并统一比较与赋值。 |

### 🛰️ 维度四：观测链路统一性

| 编号 | 状态 | 文件:行号 | 问题描述 | 严重程度 | 修复建议 |
|------|------|-----------|----------|----------|----------|
| O-001 | [x] | `api/handlers/agent.go:339` | 流式执行失败分支未记录 `execErr`，但后续仍输出 `agent stream completed`，观测语义冲突。 | P1 | 失败分支补 `logger.Error` 并区分 completion 日志语义。 |
| O-002 | [x] | `internal/app/bootstrap/bootstrap.go:96` | logger 初始化失败时用 `fmt.Printf`，绕过统一日志管道与字段规范。 | P2 | 使用结构化 fallback logger（或统一 stderr adapter）。 |

### 🔗 维度五：前后端契约一致性

| 编号 | 状态 | 文件:行号 | 问题描述 | 严重程度 | 修复建议 |
|------|------|-----------|----------|----------|----------|
| C-001 | [x] | `api/routes/routes.go:90` | Protocol 路由均为 path-only，`api/handlers/protocol.go` 无方法校验；与 OpenAPI 中显式 method 契约不严格一致（`api/openapi.yaml:969,1011,1084`）。 | P1 | 将 protocol 路由改为 `GET/POST` method-aware 声明。 |
| C-002 | [x] | `api/handlers/agent.go:341` | SSE error 事件仅返回 `{\"error\":\"agent execution failed\"}`，缺少 `code/request_id`，与统一错误 envelope 不一致。 | P2 | 统一 SSE error schema（至少 `code/message/request_id`）。 |

### ⚡ 维度六：性能与资源泄漏

| 编号 | 状态 | 文件:行号 | 问题描述 | 严重程度 | 修复建议 |
|------|------|-----------|----------|----------|----------|
| P-001 | [x] | `agent/handoff/protocol.go:193` | `pending` map 持续写入 `resultCh`，当前文件无 `delete(m.pending, id)` 清理路径，存在长期泄漏风险。 | P1 | 在完成/拒绝/超时/取消路径统一删除并关闭 channel。 |

### 🔒 维度七：安全漏洞统一性

| 编号 | 状态 | 文件:行号 | 问题描述 | 严重程度 | 修复建议 |
|------|------|-----------|----------|----------|----------|
| X-001 | [x] | `pkg/middleware/middleware.go:545` | JWT parser 未启用 `WithExpirationRequired()`；运行复核显示“无 `exp` token 可通过认证”。 | P0 | 添加 `jwt.WithExpirationRequired()` 并补无 `exp` 拒绝单测。 |
| X-002 | [x] | `agent/discovery/protocol.go:349` | 500 错误直接回传 `err.Error()`，可能暴露内部错误细节。 | P1 | 对外返回通用消息，细节仅写日志。 |

### 🔄 维度八：并发与事务一致性

| 编号 | 状态 | 文件:行号 | 问题描述 | 严重程度 | 修复建议 |
|------|------|-----------|----------|----------|----------|
| T-001 | [x] | `agent/discovery/service.go:106` | `running` 在 `Start/Stop` 间读写未受互斥保护，且 `done/closeOnce` 生命周期不可重置，重复启停有竞态风险。 | P1 | 为状态读写加锁，并在重启场景重建生命周期通道。 |

### 📐 维度九：代码分层一致性

| 编号 | 状态 | 文件:行号 | 问题描述 | 严重程度 | 修复建议 |
|------|------|-----------|----------|----------|----------|
| L-001 | [x] | `api/routes/routes.go:37` | 同一 `routes` 层混用 path-only 与 method-aware 两套范式（agent/protocol vs provider/tools），方法约束职责分散。 | P2 | 统一为 method-aware 路由注册策略。 |

---

## 💡 系统性改进方案

### 短期修复（直接修复具体问题）
- [ ] 立即修复 `X-001`（JWT exp 强制），补安全单测。
- [ ] 修复 `V-001` 与 `X-002`（announce 入参上限 + 错误脱敏）。
- [ ] 修复 `P-001`（handoff pending 生命周期清理）。
- [ ] 修复 `O-001` 与 `C-002`（SSE 错误事件与日志语义统一）。

### 中期防护（自动化检查机制）
- [ ] 增加静态规则：禁止新增 path-only 的 mutation API 路由。
- [ ] 增加契约测试：校验 protocol/agent SSE 错误模型字段完整性。
- [ ] 增加启停一致性测试：`Start -> Stop -> Start` 回归用例。

### 长期治理（架构层面提升）
- [ ] 统一路由声明规范（全量 method-aware + OpenAPI 同源校验）。
- [ ] 建立状态枚举词典，禁止流程状态裸字符串散落。
- [ ] 建立资源生命周期模板（map/channel/goroutine）并纳入评审基线。

---

## 📝 修复记录

| 日期 | 编号 | 修复人/工具 | 修复说明 | 关联 commit |
|------|------|-------------|----------|-------------|
| | | | | |
| 2026-03-04 | E-001 | /bug-manager | 已完成代码修复与回归：JWT exp 强制、Discovery 入参上限与错误脱敏、路由 method-aware 收敛、SSE 错误模型统一、Handoff pending 清理、Discovery Service/Protocol 生命周期可重启、PlanExecute 状态常量化、APIKeyPool 构造错误返回、Bootstrap 结构化 fallback logger。 | - |
| 2026-03-04 | V-001 | /bug-manager | 已完成代码修复与回归：JWT exp 强制、Discovery 入参上限与错误脱敏、路由 method-aware 收敛、SSE 错误模型统一、Handoff pending 清理、Discovery Service/Protocol 生命周期可重启、PlanExecute 状态常量化、APIKeyPool 构造错误返回、Bootstrap 结构化 fallback logger。 | - |
| 2026-03-04 | V-002 | /bug-manager | 已完成代码修复与回归：JWT exp 强制、Discovery 入参上限与错误脱敏、路由 method-aware 收敛、SSE 错误模型统一、Handoff pending 清理、Discovery Service/Protocol 生命周期可重启、PlanExecute 状态常量化、APIKeyPool 构造错误返回、Bootstrap 结构化 fallback logger。 | - |
| 2026-03-04 | S-001 | /bug-manager | 已完成代码修复与回归：JWT exp 强制、Discovery 入参上限与错误脱敏、路由 method-aware 收敛、SSE 错误模型统一、Handoff pending 清理、Discovery Service/Protocol 生命周期可重启、PlanExecute 状态常量化、APIKeyPool 构造错误返回、Bootstrap 结构化 fallback logger。 | - |
| 2026-03-04 | S-002 | /bug-manager | 已完成代码修复与回归：JWT exp 强制、Discovery 入参上限与错误脱敏、路由 method-aware 收敛、SSE 错误模型统一、Handoff pending 清理、Discovery Service/Protocol 生命周期可重启、PlanExecute 状态常量化、APIKeyPool 构造错误返回、Bootstrap 结构化 fallback logger。 | - |
| 2026-03-04 | O-001 | /bug-manager | 已完成代码修复与回归：JWT exp 强制、Discovery 入参上限与错误脱敏、路由 method-aware 收敛、SSE 错误模型统一、Handoff pending 清理、Discovery Service/Protocol 生命周期可重启、PlanExecute 状态常量化、APIKeyPool 构造错误返回、Bootstrap 结构化 fallback logger。 | - |
| 2026-03-04 | O-002 | /bug-manager | 已完成代码修复与回归：JWT exp 强制、Discovery 入参上限与错误脱敏、路由 method-aware 收敛、SSE 错误模型统一、Handoff pending 清理、Discovery Service/Protocol 生命周期可重启、PlanExecute 状态常量化、APIKeyPool 构造错误返回、Bootstrap 结构化 fallback logger。 | - |
| 2026-03-04 | C-001 | /bug-manager | 已完成代码修复与回归：JWT exp 强制、Discovery 入参上限与错误脱敏、路由 method-aware 收敛、SSE 错误模型统一、Handoff pending 清理、Discovery Service/Protocol 生命周期可重启、PlanExecute 状态常量化、APIKeyPool 构造错误返回、Bootstrap 结构化 fallback logger。 | - |
| 2026-03-04 | C-002 | /bug-manager | 已完成代码修复与回归：JWT exp 强制、Discovery 入参上限与错误脱敏、路由 method-aware 收敛、SSE 错误模型统一、Handoff pending 清理、Discovery Service/Protocol 生命周期可重启、PlanExecute 状态常量化、APIKeyPool 构造错误返回、Bootstrap 结构化 fallback logger。 | - |
| 2026-03-04 | P-001 | /bug-manager | 已完成代码修复与回归：JWT exp 强制、Discovery 入参上限与错误脱敏、路由 method-aware 收敛、SSE 错误模型统一、Handoff pending 清理、Discovery Service/Protocol 生命周期可重启、PlanExecute 状态常量化、APIKeyPool 构造错误返回、Bootstrap 结构化 fallback logger。 | - |
| 2026-03-04 | X-001 | /bug-manager | 已完成代码修复与回归：JWT exp 强制、Discovery 入参上限与错误脱敏、路由 method-aware 收敛、SSE 错误模型统一、Handoff pending 清理、Discovery Service/Protocol 生命周期可重启、PlanExecute 状态常量化、APIKeyPool 构造错误返回、Bootstrap 结构化 fallback logger。 | - |
| 2026-03-04 | X-002 | /bug-manager | 已完成代码修复与回归：JWT exp 强制、Discovery 入参上限与错误脱敏、路由 method-aware 收敛、SSE 错误模型统一、Handoff pending 清理、Discovery Service/Protocol 生命周期可重启、PlanExecute 状态常量化、APIKeyPool 构造错误返回、Bootstrap 结构化 fallback logger。 | - |
| 2026-03-04 | T-001 | /bug-manager | 已完成代码修复与回归：JWT exp 强制、Discovery 入参上限与错误脱敏、路由 method-aware 收敛、SSE 错误模型统一、Handoff pending 清理、Discovery Service/Protocol 生命周期可重启、PlanExecute 状态常量化、APIKeyPool 构造错误返回、Bootstrap 结构化 fallback logger。 | - |
| 2026-03-04 | L-001 | /bug-manager | 已完成代码修复与回归：JWT exp 强制、Discovery 入参上限与错误脱敏、路由 method-aware 收敛、SSE 错误模型统一、Handoff pending 清理、Discovery Service/Protocol 生命周期可重启、PlanExecute 状态常量化、APIKeyPool 构造错误返回、Bootstrap 结构化 fallback logger。 | - |

---

## ✅ 归档信息

> 📅 归档时间：2026-03-04 15:32:56
> 📊 修复统计：全部已修复
> 🏷️ 归档原因：所有 Bug 已修复完成
