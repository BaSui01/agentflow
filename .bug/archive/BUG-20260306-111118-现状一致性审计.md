# 🔍 Bug 审计报告：项目现状一致性审计

> 📅 创建时间：2026-03-06 11:11:18
> 🔖 审计维度：代码分层一致性、错误捕获统一性、输入准入统一性
> 📊 总体评分：6.8/10

---

## 📊 总览仪表盘

| 维度 | 评分 | P0 | P1 | P2 |
|------|------|----|----|-----|
| 🚨 错误捕获 | 7/10 | 0 个 | 1 个 | 0 个 |
| 🛡️ 输入准入 | 8/10 | 0 个 | 0 个 | 0 个 |
| 🧩 状态机 | 8/10 | 0 个 | 0 个 | 0 个 |
| 🛰️ 观测链路 | 8/10 | 0 个 | 0 个 | 0 个 |
| 🔗 前后端契约 | 8/10 | 0 个 | 0 个 | 0 个 |
| ⚡ 性能资源 | 8/10 | 0 个 | 0 个 | 0 个 |
| 🔒 安全漏洞 | 8/10 | 0 个 | 0 个 | 0 个 |
| 🔄 并发事务 | 8/10 | 0 个 | 0 个 | 0 个 |
| 📐 代码分层 | 5/10 | 0 个 | 2 个 | 2 个 |
| **总计** | **6.8/10** | **0 个** | **3 个** | **2 个** |

---

## 📋 Bug 清单

### 🚨 维度一：错误捕获统一性

| 编号 | 状态 | 文件:行号 | 问题描述 | 严重程度 | 修复建议 |
|------|------|-----------|----------|----------|----------|
| E-001 | [x] | `agent/structured/validator_property_test.go:211` | `TestProperty_SchemaValidation_MultipleErrorsHavePaths` 使用 3 个随机字段名，但未约束互异；当 `field1/field2/field3` 碰撞时，`AddProperty`/`AddRequired` 的有效约束数下降，导致断言 `len(errors) >= 2` 偶发失败。`go test ./...` 本次实测已出现一次失败；同一测试 `-count=30` 又可通过，符合脆弱/随机测试特征。 | P1 | 将属性测试改为“先生成 3 个不同字段名”或在生成后去重重抽；避免把随机碰撞误判为实现缺陷。 |

### 🛡️ 维度二：输入准入统一性

当前轮次未发现新增高价值问题。

### 🧩 维度三：状态机统一性

当前轮次未发现新增高价值问题。

### 🛰️ 维度四：观测链路统一性

当前轮次未发现新增高价值问题。

### 🔗 维度五：前后端契约一致性

当前轮次未发现新增高价值问题。

### ⚡ 维度六：性能与资源泄漏

当前轮次未发现新增高价值问题。

### 🔒 维度七：安全漏洞统一性

当前轮次未发现新增高价值问题。

### 🔄 维度八：并发与事务一致性

当前轮次未发现新增高价值问题。

### 📐 维度九：代码分层一致性

| 编号 | 状态 | 文件:行号 | 问题描述 | 严重程度 | 修复建议 |
|------|------|-----------|----------|----------|----------|
| L-001 | [x] | `architecture_guard_test.go:411` `architecture_guard_test.go:421` `README.md:63` | `extractCmdAgentflowDocumentedFiles` 用“首个包含 `cmd/agentflow/` 的行”作为 README 结构段起点，但 README 在启动链说明中更早出现该字符串，导致守卫从第 63 行开始误扫整个目录树，最终把 `types/llm/rag/workflow` 的 `.go` 文件误报为 `cmd/agentflow` 文档陈旧；这正是 `go test ./...` 当前失败的直接原因。 | P1 | 守卫解析应锚定目录树代码块中的 `├── cmd/agentflow/`，不要使用宽松的 `Contains` 首次命中。 |
| L-002 | [x] | `scripts/arch_guard.ps1:13` `scripts/arch_guard.ps1:29` | `arch_guard.ps1` 在 `go-arch-lint` 缺失时仅输出 warning，并在非严格模式下继续返回成功；本机实测显示依赖方向检查被跳过但脚本仍然 `passed`。这意味着“守卫通过”并不等价于“依赖方向已校验”。 | P1 | 将 CI 和本地默认执行都收敛为严格模式，或在脚本内将缺失 `go-arch-lint` 视为失败。 |
| L-003 | [x] | `scripts/arch_guard.ps1:13` `scripts/arch_guard.ps1:58` | `Get-GoProductionFiles` 未排除 `.snow/`，实际把 `.snow/gen_reachability_calls.go` 当作生产包扫描；脚本因此产生 `[SPLIT] .snow is a single-file production package` 假阳性。 | P2 | 在文件收集阶段显式排除 `.snow/`、生成产物目录与仓库根演示文件。 |
| L-004 | [x] | `docs/重构计划/重构计划审核与完善-2026-03-02.md:117` | 文档建议的命令 `go test -run ArchitectureGuard ./...` 与当前测试命名不匹配；本机执行结果为各包 `no tests to run`，不能真正覆盖任何架构守卫。 | P2 | 改为显式命中现有测试名，例如 `go test -run "Test(ReadmeCmdAgentflowStructureConsistency|DependencyDirectionGuards|APIHandlerInfraImportGuards|CmdEntrypointImportAllowlist|AgentRootPackageFileBudget)" .`，或新增统一的 `TestArchitectureGuard_*` 命名。 |

---

## 🧪 本次验证证据

- `python scripts/refactor_plan_guard.py report`：`docs/重构计划/未使用代码治理重构计划-2026-03-03.md` 仍有 `9` 项未完成，项目不能判定为“已收尾”。
- `python scripts/refactor_plan_guard.py gate`：明确阻断收尾，提示同一份计划仍有未完成任务。
- `go test ./...`：现已通过，README 守卫误报与 `agent/structured` 属性测试脆弱性已消除。
- `go test -run TestReadmeCmdAgentflowStructureConsistency -count=1 .`：通过，证明 README 结构守卫已锚定到正确目录树段。
- `go test ./agent/structured -run TestProperty_SchemaValidation_MultipleErrorsHavePaths -count=50`：通过，证明随机字段名碰撞问题已修复。
- `powershell -ExecutionPolicy Bypass -File scripts/arch_guard.ps1`：现为 fail-closed；在当前环境缺失 `go-arch-lint` 时会直接失败，不再 warning-only 放行；同时 `.snow` 已从生产文件扫描中排除。

---

## 🎯 优先级建议

1. 已完成 `L-001`：`go test ./...` 已恢复为可信回归门。
2. 已完成 `L-002`：`arch_guard.ps1` 改为默认失败关闭。
3. 已完成 `E-001`：属性测试输入已保证字段名唯一。
4. 已完成 `L-003/L-004`：守卫扫描噪声和文档命令失真已修正。

---

## 📝 结论

事实：本轮审计识别出的 5 个问题均已修复，且 `go test ./...` 已恢复全绿。

推论：本次缺陷修复已闭环；但项目级“重构计划”仍有未完成项，不能据此推断整个重构工程已经收尾。

范围与限制：本次审计以仓库现状、计划文档、`go test`、`arch_guard.ps1` 的本地执行结果为依据；未覆盖依赖漏洞扫描、外部服务联调与性能压测。

---

## ✅ 归档信息

> 📅 归档时间：2026-03-06 12:05:04
> 📊 修复统计：全部已修复
> 🏷️ 归档原因：所有 Bug 已修复完成
