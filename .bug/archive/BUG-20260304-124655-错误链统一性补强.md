# 🔍 Bug 审计报告：错误链统一性补强（跨链路）

> 📅 创建时间：2026-03-04 12:46:55
> 🔖 审计维度：🚨 错误捕获统一性、🛰️ 观测链路统一性
> 📊 总体评分：9/10

---

## 📊 总览仪表盘

| 维度 | 评分 | P0 | P1 | P2 |
|------|------|----|----|-----|
| 🚨 错误捕获 | 9/10 | 0 | 2 | 0 |
| 🛰️ 观测链路 | 8/10 | 0 | 0 | 1 |
| **总计** | **9/10** | **0** | **2** | **1** |

---

## 📋 Bug 清单

### 🚨 维度一：错误捕获统一性

| 编号 | 状态 | 文件:行号 | 问题描述 | 严重程度 | 修复建议 |
|------|------|-----------|----------|----------|----------|
| E-20260304-001 | [x] | `llm/providers/*` 多处 | `&types.Error{Message: err.Error()}` 未附带 `Cause`，导致错误链不可解包 | P1 | 统一补齐 `Cause: err` |
| E-20260304-002 | [x] | `pkg/migration/migrator.go:403` | 聚合错误使用 `%v`，丢失 unwrap 链路 | P1 | 改为 `errors.Join(errs...)` + `%w` |

### 🛰️ 维度四：观测链路统一性

| 编号 | 状态 | 文件:行号 | 问题描述 | 严重程度 | 修复建议 |
|------|------|-----------|----------|----------|----------|
| O-20260304-001 | [ ] | 全仓剩余少量 `%v` | 少量 `%v` 用于非 error 数据打印（如 IDs/切片/panic any），非错误链问题 | P2 | 保持现状，避免误改语义 |

---

## 📝 修复记录

| 日期 | 编号 | 修复人/工具 | 修复说明 | 关联 commit |
|------|------|-------------|----------|-------------|
| 2026-03-04 | E-20260304-001 | Codex | 批量补齐 `Cause: err`（providers + base + embedding）并通过测试 | N/A |
| 2026-03-04 | E-20260304-002 | Codex | `migrator.Close` 改为 `errors.Join + %w` 并通过测试 | N/A |

## 🔁 2026-03-04 非 Provider 链路复扫

- 复扫范围：排除 `llm/providers/**` 后的所有 `*.go`（非测试文件）。
- 规则：定位 `&types.Error{... Message: err.Error() ...}` 且检查是否缺少 `Cause`。
- 结论：未发现新增缺陷。
  - 命中仅 1 处：[llm/capabilities/embedding/base.go](E:/code/agentflow/llm/capabilities/embedding/base.go)
  - 该处已为 `Message: err.Error(), Cause: err, ...`，符合规则。

## 🔁 2026-03-04 MCPError 可追踪性统一

- 修复对象：`agent/protocol/mcp/MCPError`（非 `types.Error`）
- 改动：
  - `MCPError` 新增 `Cause error`（`json:"-"`）
  - 实现 `Error()` 与 `Unwrap()`，支持 `errors.Is/errors.As`
  - 新增 `NewMCPErrorValue(...)`、`NewMCPInternalError(err)` 统一构造
  - `server.go` 内部错误分支统一改为 `NewMCPInternalError(err)`
- 验证：
  - `go test ./agent/protocol/mcp -count=1` 通过
  - 新增测试覆盖 `cause` 透传与 JSON 不泄漏 cause 字段

## 🔁 2026-03-04 第三轮自动筛查（三类规则）

- 扫描规则：
  - `fmt.Errorf(...%v...)`（关注 error 包装丢链）
  - `recover()` 分支（关注 panic 载荷 error 链保留）
  - `zap.String("error", err.Error())`
- 判定结果：
  - `%v` 命中 17 处，均为非 error 对象打印或 panic 非 error 载荷转换（如时间、枚举、切片、ID）；未发现可直接替换为 `%w` 的安全批量项。
  - `recover()` 命中 11 处；其中 7 处已可追踪链路，4 处仅记录 `panic any`。
  - `zap.String("error", err.Error())` 命中 0 处。
- 本轮修复（低风险日志增强，不改业务流）：
  - `config/hotreload.go`：rollback callback panic 日志增加 `zap.Error(panicPayloadToError(r))`
  - `agent/event.go`：event handler panic 日志增加 `zap.Error(panicPayloadToError(r))`
  - `agent/discovery/registry.go`：event handler panic 日志增加 `zap.Error(recoveredPanicToError(rec))`
  - `llm/runtime/router/apikey_pool.go`：两处 async update panic 日志增加 `zap.Error(recoveredPanicToError(r))`
  - `pkg/middleware/middleware.go`：Recovery 日志增加 `zap.Error(recoveredPanicToError(err))`
  - `agent/react.go`：panic 日志增加 `zap.Error(panicPayloadToError(r))`
- 验证：
  - `go test ./config ./agent/discovery ./pkg/middleware ./llm/runtime/router ./agent -count=1` 通过
  - `go test ./... -run TestNonExistent -count=1` 通过

---

## ✅ 归档信息

> 📅 归档时间：2026-03-04 15:38:39
> 📊 修复统计：全部已修复
> 🏷️ 归档原因：所有 Bug 已修复完成
