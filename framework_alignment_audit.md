# Framework Alignment Audit

| 时间 | 范围 | 官方协议变化 | 本地差异 | 已实施对齐 | 验证 |
| --- | --- | --- | --- | --- | --- |
| 2026-04-08 12:18:00 +08:00 | OpenAI / Anthropic / Gemini SDK source re-audit | OpenAI 官方 SDK / 文档当前同时暴露 prompt cache（`prompt_cache_key` / `prompt_cache_retention`），`prompt_cache_retention` 文档侧使用 `in_memory`，而生成型 Go SDK 枚举仍出现 `in-memory`；Anthropic 官方 SDK 已将 `adaptive thinking`、`thinking.display`、`output_config.effort` 收敛到主 Messages API，而 `speed=fast` 仍通过 beta 能力启用；Gemini 官方 `go-genai` 仍透传 `cachedContent + systemInstruction + toolConfig` 组合，并显式支持 `VALIDATED` 与 `includeServerSideToolInvocations` | 本地此前对 OpenAI `prompt_cache_retention` 透传过宽，允许非官方值；Anthropic `fast mode` 仅通过 header 近似模拟，未收敛到 beta message 路径语义，且 `cache_control` 缺少官方枚举校验；Gemini adapter 会在存在 `systemInstruction/tools/toolConfig` 时主动清空 `cachedContent`，比官方 SDK 更保守 | 新增统一 `NormalizeOpenAIPromptCacheRetention(...)`，兼容 `in_memory / in-memory / 24h` 输入并统一输出文档形态；Anthropic adapter 新增 `cache_control` 规范化校验，仅允许 `ephemeral + ttl(5m/1h)`，并在 `fast` 场景走 beta message endpoint 语义（含 `?beta=true` + beta header）；移除 Gemini `cachedContent` 主动清洗逻辑，保留与官方 SDK一致的请求透传；同步 OpenAPI / API 类型 / 中文 API 文档与对应回归测试 | `go test ./llm/providers/base ./llm/providers/openai ./llm/providers/openaicompat ./llm/providers/anthropic ./llm/providers/gemini ./api/handlers` 通过；`go test ./...` 通过；`powershell -ExecutionPolicy Bypass -File scripts/arch_guard.ps1` 通过 |
| 2026-04-08 03:50:53 +08:00 | OpenAI / Anthropic / Gemini chat-tooling-stream-cache | OpenAI 新增 strict tool schema 与 prompt cache routing (`prompt_cache_key` / `prompt_cache_retention`)；Anthropic tool use 文档已切到 `web_search_20260209`，并支持 strict tool schema 与 automatic prompt caching `cache_control`；Gemini function calling 文档新增 `VALIDATED` / `includeServerSideToolInvocations`，context caching 使用 `cachedContent` | `types.ToolSchema` 无 strict；`llm.ChatRequest` / `api.ChatRequest` 无 OpenAI prompt cache、Anthropic `cache_control`、Gemini `cached_content` 与 server-side tool invocation 控制；Anthropic 仍注入 `web_search_20250305`；Anthropic 未根据统一 `ParallelToolCalls` 透传 `disable_parallel_tool_use` | 为公共 ToolSchema 增加 `strict`；为统一请求模型增加 `prompt_cache_key`、`prompt_cache_retention`、`cache_control`、`cached_content`、`include_server_side_tool_invocations`；同步 `api/handlers`、OpenAI compat、OpenAI Responses、Anthropic、Gemini adapter 映射；Anthropic server tool 升级到 `web_search_20260209`；Gemini tool config 支持 `VALIDATED` 和 allowlist；新增/更新对应测试与 OpenAPI 文档 | `go test ./api/handlers ./llm/providers/base ./llm/providers/openaicompat ./llm/providers/openai ./llm/providers/anthropic ./llm/providers/gemini` 通过；`go test ./...` 通过；`powershell -ExecutionPolicy Bypass -File scripts/arch_guard.ps1` 通过 |
| 2026-04-08 04:09:21 +08:00 | Anthropic adaptive thinking / Gemini cachedContent degradation | Anthropic 2026-02-05 起将思考协议切到 `thinking.type: "adaptive"`，并新增 `output_config.effort`、`thinking.display`、`speed`；`budget_tokens` 仍存在但已不再是新模型的主控方式。Gemini `cachedContent` 继续作为上下文缓存入口，但缓存型上下文与按次传入的 `systemInstruction/tools/toolConfig` 组合会导致请求约束收紧 | Claude adapter 仍把新模型统一降到旧的 `thinking.enabled + budget_tokens`；统一请求层未透出 `reasoning_display` / `inference_speed`；Gemini adapter 虽已支持 `cachedContent`，但未在冲突组合场景下做无崩溃降级 | 在统一请求模型与 API 层新增 `reasoning_display`、`inference_speed`；Anthropic adapter 升级为单一实现：优先走 `adaptive + output_config.effort`，对不支持 `adaptive/fast` 的模型自动回退到旧 thinking 或标准速度，并在 `429` fast-mode 场景自动重试一次标准速度；Gemini adapter 增加 `cachedContent` 冲突清洗，检测到 `systemInstruction/tools/toolConfig` 并存时主动放弃缓存复用，保留请求语义避免 400 | `go test ./llm/providers/anthropic ./llm/providers/gemini ./api/handlers` 通过；后续全量 `go test ./...` 与架构守卫通过 |
| 2026-04-08 05:03:44 +08:00 | OpenAI custom tools / custom_tool_call | OpenAI 2026-01-15 起在 Responses 与 Chat Completions 文档中公开 `custom` tools、`custom_tool_call`、`custom_tool_call_output`，并允许通过 grammar/regex format 约束非 JSON 文本输入；流式事件新增 `response.custom_tool_call_input.delta/done` | 本地统一 `ToolSchema` 仅表达 `function` 型工具；`ToolCall` 仅支持 JSON `arguments`；OpenAI Responses builder、stream parser、OpenAI-compatible 入站/出站转换都只处理 `function_call` / `function_call_output`，无法 round-trip `custom` tools | 在统一抽象层新增 `ToolSchema.Type/Format` 与 `ToolCall.Type/Input`；OpenAI Responses adapter 支持 `custom` tool 定义、`custom_tool_call`/`custom_tool_call_output` 输入输出与流式聚合；OpenAI-compatible base 与 API handler 同步支持 custom tool 的 inbound/outbound 转换；OpenAPI 与中文 API 文档同步更新 | `go test ./llm/providers/base ./llm/providers/openai ./api/handlers` 通过；`go test ./...` 通过；`powershell -ExecutionPolicy Bypass -File scripts/arch_guard.ps1` 通过 |
