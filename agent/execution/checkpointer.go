// checkpointer.go is intentionally empty.
// The Checkpointer interface, MemoryCheckpointer, ThreadManager, and related
// types were removed because they had zero external consumers.
// Use agent.CheckpointStore or workflow.CheckpointStore instead.
package execution
