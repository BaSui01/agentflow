# 示例：架构重构计划（包合并/唯一入口/目录治理）

> 文档类型：可执行架构重构计划
> 适用场景：包拆分、包合并、重复功能统一、唯一入口收口、根目录治理
> 创建日期：2026-04-22
> 执行规则：单轨替换；不保留兼容分支；不保留双实现
> 测试规则：TDD（Red-Green-Refactor）
> 停止规则：存在 `- [ ]` 或验证未通过时不得宣布完成

---

## 0. 执行状态总览

- [x] 完成现状盘点与结构基线
- [x] 完成职责矩阵与重复语义归类
- [ ] 完成唯一正式入口与目标架构定义
- [ ] 完成 Phase 拆分与删除清单
- [ ] 完成 TDD 守卫与最小实现落地
- [ ] 完成文档、守卫与 DoD 验收

---

## 1. 重构范围与目标

### 1.1 目标范围

- [ ] 目标包/模块：`agent/runtime/`
- [ ] 关联目录/文档/守卫：`README.md`、`docs/architecture/`、`architecture_guard_test.go`

### 1.2 业务/使用目标

- [ ] 减少调用方选择成本（从 3 个入口降到 1 个）
- [ ] 统一 runtime 构造主路径

### 1.3 架构目标

- [ ] 明确唯一正式入口：`agent.Builder`
- [ ] 明确重复语义的唯一归口：
  - 构造 → `agent.Builder`
  - 注册 → `agent.Registry`
  - 运行时桥接 → `agent/runtime/bridge.go`
- [ ] 明确保留/合并/下沉/删除边界
- [ ] 不涉及根目录治理（本轮仅处理 `agent/` 包内部）

### 1.4 非目标

- [ ] 不在本轮处理 `workflow/` 包
- [ ] 不在本轮处理 `api/handlers/` 层

---

## 2. 现状问题（证据）

- [x] 问题 1：存在 3 个并行构造入口（证据：`agent/builder.go:15`, `agent/factory.go:20`, `agent/runtime/legacy.go:30`）
- [x] 问题 2：`factory.go` 和 `legacy.go` 职责重复（证据：两者都实现 Agent 构造逻辑）
- [x] 问题 3：调用方混用多个入口（证据：`cmd/agentflow/main.go:45` 同时使用 `Builder` 和 `factory.New`）

### 2.1 职责矩阵

| 对象 | 当前职责 | 问题类型 | 处理动作 | 目标归口 | 备注 |
| --- | --- | --- | --- | --- | --- |
| `agent/builder.go` | 构造 + 配置 | 职责单一 | 保留 | `agent/builder.go` | 唯一正式入口 |
| `agent/factory.go` | 构造 + 运行时桥接 | 职责混合 | 合并到 builder | `agent/builder.go` | 删除文件 |
| `agent/runtime/legacy.go` | 旧构造入口 | 并行入口 | 删除 | N/A | 完全删除 |
| `agent/runtime/bridge.go` | 运行时桥接 | 职责单一 | 保留 | `agent/runtime/bridge.go` | 保留内部构件 |

### 2.2 重复语义清单

- [x] 重复语义 1：Agent 构造（证据：`agent/builder.go:15`, `agent/factory.go:20`, `agent/runtime/legacy.go:30`；唯一归口：`agent/builder.go`）
- [x] 重复语义 2：运行时桥接（证据：`agent/factory.go:50`, `agent/runtime/bridge.go:20`；唯一归口：`agent/runtime/bridge.go`）

### 2.3 当前结构基线

- [x] 统计目标目录当前文件数
  - 验证命令：`rg --files agent/ | wc -l`
  - 通过标准：基线为 15 个文件（已记录）

- [x] 统计当前入口数
  - 验证命令：`rg "func New.*Agent" agent/`
  - 通过标准：基线为 3 个入口（已记录）

---

## 3. 目标架构

### 3.1 唯一正式入口

- [ ] 唯一正式入口：`agent.Builder`
- [ ] 旧正式入口降级或删除：
  - `agent/factory.go` → 删除
  - `agent/runtime/legacy.go` → 删除

### 3.2 目标分层/分包

- [ ] 保留：`agent/builder.go`（唯一构造入口）
- [ ] 保留：`agent/runtime/bridge.go`（内部运行时桥接）
- [ ] 删除：`agent/factory.go`
- [ ] 删除：`agent/runtime/legacy.go`

### 3.3 依赖方向与主链约束

- [ ] 不破坏现有层级依赖方向（`agent/` 不依赖 `cmd/` 或 `api/`）
- [ ] 不绕过 `cmd -> bootstrap -> api -> domain` 启动主链
- [ ] 不在 handler/adapter 层回灌业务实现

---

## 4. 测试策略（TDD）

- [ ] 先写失败测试并确认红灯
  - 验证命令：`go test ./agent -run TestUniqueBuilderEntry -count=1`
  - 通过标准：测试失败，错误信息显示"发现多个构造入口"

- [ ] 采用最小实现让测试转绿
  - 验证命令：`go test ./agent -run TestUniqueBuilderEntry -count=1`
  - 通过标准：测试通过，仅保留 `agent.Builder` 单一入口

- [ ] 完成重构并执行回归验证
  - 验证命令：`go test ./agent -count=1 && powershell -ExecutionPolicy Bypass -File scripts/arch_guard.ps1`
  - 通过标准：所有测试与架构守卫通过，旧实现已删除

### 4.1 建议优先补的失败测试

- [ ] 唯一入口守卫测试（检测旧入口残留）
- [ ] 结构预算守卫测试（检测 `agent/` 包文件数是否符合预算）
- [ ] 依赖方向守卫测试（禁止 `agent/` 依赖 `cmd/` 或 `api/`）

---

## 5. 执行计划（Phase）

### Phase-0：冻结与基线

- [x] 冻结非重构改动
  - 验证命令：`git status --short`
  - 通过标准：仅保留本次重构相关改动

- [x] 固化测试基线
  - 验证命令：`go test ./agent -count=1`
  - 通过标准：当前基线测试全部通过（已记录）

- [x] 固化结构基线
  - 验证命令：`rg --files agent/ | wc -l`
  - 通过标准：当前 15 个文件（已记录）

### Phase-1：职责矩阵与唯一归口

- [ ] 为每个目标文件标记"保留/合并/下沉/删除"
  - 验证命令：人工审阅计划文档
  - 通过标准：职责矩阵无未归类项（见 2.1 节）

- [ ] 为每类重复语义定义唯一归口
  - 验证命令：人工审阅计划文档
  - 通过标准：每类重复语义只有一个正式归口（见 2.2 节）

- [ ] 明确唯一正式入口与旧入口处理方式
  - 验证命令：`rg "factory.New|legacy.New" agent/`
  - 通过标准：目标入口收口方案已明确（见 3.1 节）

### Phase-2：失败测试先落地

- [ ] 补充唯一入口守卫测试（先失败）
  - 验证命令：`go test ./agent -run TestUniqueBuilderEntry -count=1`
  - 通过标准：测试失败，准确检测到多入口问题

- [ ] 记录转绿最小条件
  - 验证命令：计划文档人工审阅
  - 通过标准：转绿条件为"只保留 `agent.Builder`，删除 `factory.New` 和 `legacy.New`"

### Phase-3：包合并/删除落地

- [ ] 合并 `factory.go` 功能到 `builder.go`
  - 验证命令：`go test ./agent -count=1`
  - 通过标准：功能已合并，测试通过

- [ ] 删除 `agent/factory.go`
  - 验证命令：`Test-Path agent/factory.go`
  - 通过标准：文件不存在

- [ ] 删除 `agent/runtime/legacy.go`
  - 验证命令：`Test-Path agent/runtime/legacy.go`
  - 通过标准：文件不存在

- [ ] 更新所有调用方使用 `agent.Builder`
  - 验证命令：`rg "factory.New|legacy.New" cmd/ api/`
  - 通过标准：0 命中

### Phase-4：守卫与文档同步

- [ ] 更新 README/README_EN 中的入口说明
  - 验证命令：`rg "factory.New|legacy.New" README.md README_EN.md`
  - 通过标准：0 命中旧口径

- [ ] 更新 `architecture_guard_test.go`
  - 验证命令：`go test ./agent -run PlanGuard -count=1`
  - 通过标准：守卫口径与新结构一致

- [ ] 更新 `docs/architecture/` 架构文档
  - 验证命令：`rg "factory|legacy" docs/architecture/`
  - 通过标准：0 命中旧口径

### Phase-5：验收与收尾

- [ ] 相关测试通过
  - 验证命令：`go test ./agent -count=1`
  - 通过标准：退出码为 0

- [ ] 架构守卫通过
  - 验证命令：`powershell -ExecutionPolicy Bypass -File scripts/arch_guard.ps1`
  - 通过标准：退出码为 0

- [ ] 计划 lint/report 通过
  - 验证命令：`python scripts/refactor_plan_guard.py lint --target "architecture-refactor-plan.md" --require-tdd --require-verifiable-completion`
  - 通过标准：结构合法，进度可核对

---

## 6. 删除清单

- [ ] 删除旧正式入口：`agent/factory.go`
  - 验证命令：`rg "factory.New" agent/`
  - 通过标准：0 命中

- [ ] 删除旧正式入口：`agent/runtime/legacy.go`
  - 验证命令：`rg "legacy.New" agent/`
  - 通过标准：0 命中

- [ ] 删除旧文档口径：`README.md`、`README_EN.md`、`docs/architecture/`
  - 验证命令：`rg "factory|legacy" README.md README_EN.md docs/architecture/`
  - 通过标准：0 命中

---

## 7. 完成定义（DoD）

- [ ] 职责矩阵已闭环
  - 验证命令：人工核对计划中的职责矩阵
  - 通过标准：所有对象均已有最终归口，无"待定/暂存"项

- [ ] 重复语义已完成统一归口
  - 验证命令：人工核对重复语义清单 + 代码搜索
  - 通过标准：每类重复语义只剩一个正式实现

- [ ] 唯一正式入口已收口
  - 验证命令：`rg "agent.Builder" README.md README_EN.md docs/`
  - 通过标准：正式文档与生产代码只保留一个正式入口口径

- [ ] 无并行旧路径残留
  - 验证命令：`rg "factory.New|legacy.New" agent/ cmd/ api/`
  - 通过标准：0 命中

- [ ] 新增与回归测试全部通过
  - 验证命令：`go test ./agent -count=1`
  - 通过标准：所有测试通过

- [ ] 文档与代码一致
  - 验证命令：`python scripts/refactor_plan_guard.py report --target "architecture-refactor-plan.md"`
  - 通过标准：计划状态、证据路径、实际代码变更保持一致

- [ ] 所有任务状态均为 `[x]`
  - 验证命令：`python scripts/refactor_plan_guard.py gate --target "architecture-refactor-plan.md" --require-tdd --require-verifiable-completion`
  - 通过标准：退出码为 0，允许停止/收尾

---

## 8. 变更日志

- 2026-04-22：创建架构重构计划，完成现状盘点与职责矩阵
