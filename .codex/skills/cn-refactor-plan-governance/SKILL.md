---
name: cn-refactor-plan-governance
description: 维护并执行项目中文重构计划（docs/重构计划/*.md）的治理技能。用于计划编写、测试计划设计、状态推进、阶段验收和停止门禁。遇到“重构计划审核/推进/维护/收尾/验收/测试计划”请求时使用，强制计划包含 `- [ ]`/`- [x]` 状态，测试计划采用 TDD（先写失败测试、再最小实现、再重构），每项完成定义必须写明验证命令与通过标准，且仅在所有任务与验收标准全部通过后才允许停止。
---

# 中文重构计划治理

## 快速流程

1. 先运行严格格式检查：
   - `./scripts/run_guard.ps1 -Cmd lint -Target <计划文件名> -RequireTDD -RequireVerifiableCompletion`
2. 再看严格进度报告：
   - `./scripts/run_guard.ps1 -Cmd report -Target <计划文件名> -RequireTDD -RequireVerifiableCompletion`
3. 编写或改写测试计划时，先落 TDD 路径：
   - 先写失败测试
   - 再写最小实现让测试转绿
   - 最后重构并跑回归
4. 每次准备“结束/收尾/归档”前必须跑门禁：
   - `./scripts/run_guard.ps1 -Cmd gate -Target <计划文件名> -RequireTDD -RequireVerifiableCompletion`
5. `gate` 未通过，或计划中的验证命令/通过标准未满足时，继续推进计划项，禁止以“已完成”收尾。

## 硬规则

1. 计划文件必须包含任务状态行：`- [ ]` 或 `- [x]`。
2. 计划文件必须包含四个章节组：
   - `执行状态总览`
   - `测试策略（TDD）` 或 `测试计划（TDD）`
   - `执行计划` 或 `Phase`
   - `完成定义` 或 `DoD`
3. 测试计划必须显式写出 Red-Green-Refactor：
   - `先写失败测试`
   - `最小实现让测试转绿`
   - `重构并回归验证`
4. 每个阶段至少保留一条可验证证据，并优先写成二元验收项：
   - `验证命令：...`
   - `通过标准：...`
5. 未全部打勾（仍有 `[ ]`）或任一通过标准未满足时，不得输出“完成/停止/归档”结论。
6. 重构执行遵循单轨替换，不允许兼容双实现。

## 计划维护动作

1. 新建计划时，直接复用模板：
   - `./references/重构计划模板.md`
2. 审核计划时：
   - 先标冲突（状态与代码不一致、路径错误、守卫声明错误）
   - 再查 TDD 是否落地（是否先写失败测试、是否定义转绿条件、是否有回归步骤）
   - 最后给修订项（具体到文件与行）
3. 推进计划时：
   - 只把完成项从 `[ ]` 改为 `[x]`
   - 不删除未完成项
   - 每个 `[x]` 项都要有实际证据支撑（测试/脚本/代码路径/命令输出）
4. 判断“是否完成”时：
   - 先看所有相关 `验证命令` 是否已执行
   - 再看所有 `通过标准` 是否逐条满足
   - 全部通过了才算完成

## 资源

### scripts/

1. `run_guard.ps1`：在项目根目录调用 `scripts/refactor_plan_guard.py`，并可打开 `-RequireTDD` / `-RequireVerifiableCompletion` 严格门禁。

### references/

1. `重构计划模板.md`：规定章节和 `[ ]` 清单格式。

## 输出要求

1. 输出必须包含：
   - 当前剩余 `[ ]` 数量
   - 未完成项清单
   - 下一步执行项（仅来自现有未完成任务）
2. 如涉及测试计划，输出必须明确：
   - 失败测试是什么
   - 转绿条件是什么
   - 回归范围是什么
3. 只有 `gate` 通过且计划里的验证命令/通过标准全部通过时，才允许输出“可停止/可收尾”。
